# Build
bin/
obj/
build/
.gradle/
local.properties
*.apk
*.aab
*.keystore
*.jks

# Datos reales del cliente — contienen PII (cédula, INSS, fecha de
# nacimiento) de personal real. Nunca al control de versiones (§12.1).
# Se lee del disco local para el importador (etapa E3), nunca se commitea.
Base de Datos.xlsx

# IDE
.vs/
.idea/
*.user
.vscode/

# Secretos — nunca en el repositorio (05 TRD §3.5)
appsettings.*.local.json
.env
*.pfx

# SO
Thumbs.db
.DS_Store

# Skill externo vendorizado - no versionar su .git interno
.claude/skills/*/.git/

# Android build
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/captures/
android/.idea/
android/*.iml

# Backend build
backend/**/bin/
backend/**/obj/
backend/**/*.user

# Descargas locales usadas para bootstrap (no versionar el zip de Gradle)
