# SmartAssign — Estado de ejecución

**Se lee al empezar cada sesión. Se actualiza al terminar cada UT.**
Última actualización: 2026-08-11 · UTs completadas: **47 / 95**

## Decisiones de esta sesión que no estaban en los documentos

- **G1** cerrada: Operador A/B/C se simula tomando un subconjunto de OPERARIO (decisión del cliente); `OPERADOR DE EQUIPOS` → Operador A es **asunción propia**, revisar cuando lleguen datos reales de categorización. `OPERADOR DE CALDERAS`/`OPERARIO DE FILTROS Y TANQUERIA` → Operador A **confirmado por el cliente**.
- **G6** nueva y cerrada: `SexoPreferente` sucio en 9 filas de "Puestos Fijos" (H8) — `"Femenina"` era tipeo de `"Femenino"`, y 8 filas de la Línea 6 traían `PerfilRequerido` arrastrado por error a la columna equivocada. Reparado por patrón (confirmado por el cliente), no rechazado. Los 98 puestos reales ya importan.
- **G2** cerrada: datos médicos simulados por ahora (decisión del cliente).
- **G3** cerrada: las 10 líneas se mantienen en el modelo; L1/L2/L4/L6/L8 tienen datos reales hoy, el resto queda *inactiva* hasta que el Coordinador las planifique. `MAQUILA/PET/1605/1606` no son de las 10 líneas — quedan fuera de `linea_habitual` en el importador.
- **A14** nueva: `TiempoEnPuesto` del Excel puebla `umbral_sugerido` (renombrado `horas_en_puesto`); `umbral_critico` sigue nulable con default de planta, tal como A4 ya preveía — no hay un segundo dato real para él. Ver [00_DECISIONES.md §A14](00_DECISIONES.md).
- Skill externo `ui-ux-pro-max` vendorizado en `.claude/skills/ui-ux-pro-max-skill/` (no instalado como plugin — se invoca su script `search.py` directamente por ruta cuando llegue la etapa E6).
- **Backend apunta a .NET 10 / EF Core 10** (versión realmente instalada), no .NET 8 como decía el TRD original.
- Vulnerabilidad `NU1903` en `Microsoft.OpenApi` (dependencia transitiva del template `webapi` de .NET 10): actualizada a 2.3.0 (la más reciente disponible), sigue marcada por el feed de asesorías sin versión parcheada todavía. Rastreada en el CI como `continue-on-error`, no ignorada.

### Decisiones de ingeniería de la etapa E2 (delegadas, no de negocio — R2 no aplica)

- **AD/Entra ID no tiene adaptador todavía.** D6 dice "si la empresa lo tiene"; no hay entorno AD disponible en esta fase ni datos de conexión que nadie entregó. `IServicioAutenticacion` ya distingue `Usuario.OrigenIdentidad`; un usuario `"ad"` hoy rechaza el login con `ORIGEN_AD_NO_DISPONIBLE` en vez de fallar en silencio contra credenciales locales inexistentes. Queda listo para que un adaptador real lo implemente cuando el cliente provea el servicio.
- **Bloqueo automático por reintentos fallidos: mecanismo sin umbral.** El esquema (04 §6.1) ya trae `Usuario.IntentosFallidos`/`BloqueadoHasta`, y el login los respeta (si `BloqueadoHasta` está en el futuro, rechaza) — pero **nada fija ese campo automáticamente todavía**, porque ningún documento declara cuántos intentos ni cuántos minutos de bloqueo (no está ni en la tabla de `Parametro`, 04 §9). Inventar el número violaría R2. La tabla `Parametro` en sí **no tiene UT propia** en el plan — se creará cuando la primera regla que de verdad la necesite lo exija (candidata natural: E4 o E5).
- **El contador de PIN reutiliza `IntentosFallidos`.** El esquema declara una sola columna de reintentos por usuario, no una separada para login vs. PIN — reutilizarla es la lectura más fiel del dato declarado. Al tercer PIN fallido (04 §6.4, número explícito en el documento) se revoca la sesión activa del dispositivo y el contador se reinicia.
- **Login revoca otras sesiones activas del mismo `device_id`.** D6: "el teléfono se trata como compartido por línea". Es limpieza de concurrencia, no una regla de negocio nueva: al entrar un usuario en un teléfono, cierra cualquier sesión que hubiera quedado abierta de otro usuario en ese mismo aparato.
- **RLS (capa 3) solo cubre `Puesto` por ahora.** `JornadaLinea`, la otra tabla que 04 §6.3 menciona, no existe hasta E5 — una `SECURITY POLICY` no puede apuntar a una tabla inexistente. Su predicado se añade con `ALTER SECURITY POLICY` en la migración que cree esa tabla.
- **Primeros dos endpoints reales:** `GET /api/lineas` (coordinador) y `GET /api/lineas/{lineaId}` (coordinador o el supervisor dueño, vía `AlcanceLineaEndpointFilter`) — solo el resumen de la línea, no la malla de puestos (eso es E6). Existen para que la suite de aislamiento y PC-1 tengan algo real de la Api que golpear, y fijan el patrón (`RequireAuthorization()` + `AddEndpointFilter<AlcanceLineaEndpointFilter>()`) que todo endpoint de línea futuro debe repetir.
- **`JwtBearerOptions.MapInboundClaims = false`** — sin esto, el handler de validación remapea `"sub"` al URI largo heredado de WS-Federation y `FindFirst(ClaimsSmartAssign.UsuarioId)` deja de encontrar el claim. Gotcha conocido de ASP.NET Core, no una decisión de negocio.

> **Protocolo de sesión:**
> 1. Leer este fichero → identificar la siguiente UT sin marcar.
> 2. Leer **solo** lo que declara su columna `LEE`. Nunca la documentación entera.
> 3. Ejecutar. Si falta un dato de negocio → **detenerse y preguntar** *(regla R2)*.
> 4. Verificar en verde. Sin verde, la UT sigue abierta.
> 5. Marcar aquí + un commit citando el ID.

**Leyenda:** `[ ]` pendiente · `[~]` en curso · `[x]` verificada · `[!]` bloqueada

---

## E0 · Entorno *(3/3)* ✅

- [x] **E0.1** Fijar `JAVA_HOME` al JBR de Android Studio; comprobar Gradle y LocalDB
  - VERIFICADO: JDK 21 (JBR) responde · LocalDB `MSSQLLocalDB` responde con SQL Server 2025
- [x] **E0.2** Esqueleto de solución backend
  - VERIFICADO: `backend/SmartAssign.sln` con Domain/Application/Infrastructure/Api + 4 proyectos de prueba, referencias de Clean Architecture cableadas, `dotnet build` → 0 errores
- [x] **E0.3** Esqueleto Android + CI local
  - VERIFICADO: proyecto Compose (Kotlin, AGP 8.7.2, Compose BOM 2024.10.01, minSdk 26/targetSdk 36) · `gradlew assembleDebug` → BUILD SUCCESSFUL, `app-debug.apk` generado

## E1 · Cimientos y semilla estructural *(6/6)* ✅ → F0

- [x] **E1.1** Migración 001: líneas, puestos, catálogos base
  - VERIFICADO: migración `InicialEstructura` aplicada y revertida manualmente contra LocalDB, y de nuevo como prueba automatizada (`La_migracion_revierte_limpiamente`, `Reglas.SeguridadTests`)
- [x] **E1.2** Semilla estructural: 10 líneas, L8 Bolsón, prioridad base
  - VERIFICADO: 10 líneas, exactamente 1 con `es_bolson=1` (L8), 10 órdenes de prioridad vigentes distintos, L4 en orden 1 — por consulta directa y por prueba automatizada
- [x] **E1.3** Semilla `ProximidadLinea` — grafo dirigido, corrección A1
  - VERIFICADO: 81 filas (9 orígenes × 9 destinos; L8 sin filas como origen), sin repeticiones ni auto-referencias — prueba `Cada_una_de_las_9_lineas_origen_tiene_exactamente_9_destinos_sin_repetir`
- [x] **E1.4** Prueba dedicada de la fila de L10
  - VERIFICADO: `La_fila_de_L10_es_la_corregida_por_el_cliente` — `L9,L3,L6,L7,L4,L2,L1,L5,L8` exacto, más `La_proximidad_es_asimetrica_a_proposito_entre_L1_y_L5` (A3)
- [x] **E1.5** Semilla de capacidades físicas y catálogos de excepción/rechazo/paro
  - VERIFICADO: `CapacidadFisica` (6, placeholder — G2), `MotivoExcepcion` (7), `MotivoRechazoRecepcion` (4), `CategoriaParo`+`CausaParo` (4+5) sembrados vía `HasData`
- [x] **E1.6** Pipeline CI: build, test, arquitectura, secretos
  - VERIFICADO: `.github/workflows/backend-ci.yml` (build, arquitectura, unitarias, **reglas de seguridad bloqueante**, integración, auditoría de dependencias, TruffleHog) · corrido en local: **11/11 pruebas reales en verde**

**Nota de diseño de esta etapa:** los datos de semilla (`DatosEstructurales`, `DatosCatalogo`) se ubicaron en `SmartAssign.Domain/Semillas/`, no en `Infrastructure` — son datos de negocio (la tabla de proximidad corregida es una decisión de negocio, A1/A3), y el ORM solo los persiste vía `HasData`. Corrige un error de capa que cometí al primer intento y arreglé antes de compilar.

## E2 · Identidad y aislamiento *(6/6)* ✅ → F1

- [x] **E2.1** `Usuario`, `SesionDispositivo`, `DispositivoPush`
  - VERIFICADO: migración `IdentidadYAuditoria` aplicada y revertida (misma prueba automatizada de reversibilidad de E1, ahora cubre las 5 migraciones); FK real `Linea.SupervisorActualId → Usuario` añadida (antes columna simple)
- [x] **E2.2** JWT + refresh + PIN — **sin `linea_id` en el token**
  - VERIFICADO: `TokenClaimsTests` (4 pruebas) — `El_access_token_nunca_lleva_linea_id`, claims exactos `sub`/`rol`/`nombre`, expiración a 15 min, refresh opaco de un solo segmento; `CicloDeSesionTests` (6 pruebas) — refresh, PIN correcto, **3 PIN fallidos cierran la sesión (04 §6.4)**, PIN nunca abre sesión ajena, logout revoca refresh
- [x] **E2.3** Filtro de rol + filtro de alcance por línea
  - VERIFICADO: `AislamientoEntreSupervisoresTests` (6 pruebas, Api real vía `WebApplicationFactory`) — `Un_supervisor_de_L2_es_rechazado_en_el_endpoint_de_L4`, coordinador sin filtro, supervisor no puede listar todas, sin token → 401, supervisor sin línea asignada → 403
- [x] **E2.4** RLS en SQL Server como tercera capa
  - VERIFICADO: `AislamientoYAuditoriaTests` (3 pruebas de RLS) contra **conexión SQL cruda, sin ningún filtro de aplicación** — sin `SESSION_CONTEXT` bloquea todo (0 filas), coordinador ve todo, supervisor de L1 solo ve L1 aunque existan filas de L2. Solo cubre `Puesto`; `JornadaLinea` se añade en E5 (ver nota de ingeniería arriba)
- [x] **E2.5** `Auditoria` + `sp_RegistrarAuditoria`, **incluidos los rechazos**
  - VERIFICADO: `Un_login_exitoso_deja_una_fila_OK_en_Auditoria` y `Un_login_con_clave_equivocada_deja_una_fila_RECHAZO_en_Auditoria`, ambas vía el procedimiento almacenado real (Dapper), no un INSERT de EF
- [x] **E2.6** Suite de aislamiento completa
  - VERIFICADO: **32/32 pruebas reales en verde** en los 4 proyectos (Domain.UnitTests 4, Arquitectura.Tests 2, Api.IntegrationTests 6, Reglas.SeguridadTests 20) — incluida `Dos_supervisores_en_dos_telefonos_ninguno_ve_la_linea_del_otro`, la prueba automatizada exacta de PC-1

> **→ PC-1** · Validación humana: dos supervisores en dos teléfonos, ninguno ve la línea del otro. **Listo para validar** — requiere H2 (teléfonos físicos) y la app real (E6); por ahora demostrado por API + pruebas automatizadas.

## E3 · Personal y puestos *(6/6)* ✅ → F2

- [x] **E3.1** `Puesto` con umbrales propios nulables y `titular_id` de doble semántica
  - VERIFICADO: `CK_Puesto_umbrales` rechaza crítico=sugerido y crítico<sugerido (`PersonalYPuestosTests`), acepta crítico>sugerido y nulos. **Cambio de diseño respecto al 04 original:** el umbral crítico se guarda en horas (`umbral_critico_horas`), no minutos, para poder compararse contra `horas_en_puesto` sin conversión — ver nota de ingeniería abajo
- [x] **E3.2** `PuestoCapacidad` y `TipoActividad`
  - VERIFICADO: tabla `PuestoCapacidad` (FK a Puesto+CapacidadFisica) migrada y probada; la bandera `aplica_no_repeticion_24h` de la UT original **ya no existe** — A12 (cerrada antes de esta sesión) la sustituyó por `Puesto.horas_recuperacion` con dato real por puesto. Verificación real con las horas de Girar botellas(24)/Limpieza(48) queda para E3.6, cuando haya datos importados
- [x] **E3.3** `Personal` — `categoria` (mapeada de G1), `sexo` nulable, `linea_habitual`
  - VERIFICADO: `Personal_se_guarda_con_sexo_nulo_sin_error`. **Cambio de diseño respecto al 04 original:** el campo `perfil` (genérico) se sustituye por `sexo` — A13 (cerrada antes de esta sesión, no reflejada aún en 04) confirma que la regla blanda del §7.3 es la comparación de sexo contra `Puesto.sexo_preferente`. 04_ESQUEMA_BACKEND.md actualizado
- [x] **E3.4** `RestriccionMedica` con vigencia y sin borrado
  - VERIFICADO: `DENY_impide_borrar_una_restriccion_medica_con_la_cuenta_de_aplicacion` — impersonando un usuario real `rol_app` (`WITHOUT LOGIN`, creado en esta etapa) vía `EXECUTE AS`, con `GRANT SELECT/INSERT/UPDATE` explícito para aislar que lo único denegado es `DELETE`. Mensaje de SQL Server confirmado literalmente
- [x] **E3.5** **Semilla simulada adversaria** — los 16 escenarios
  - VERIFICADO: `SembradorAdversario` (Infrastructure) + `herramientas/ImportadorCli sembrar-adversaria` + `SemillaAdversariaTests` — 20 pruebas (16 escenarios, uno de ellos `[Theory]` por línea, más la prueba de separación de origen). Corrido de verdad contra `SmartAssignDev` tras el import real — ver el listado completo de los 16 en la nota de ingeniería abajo
- [x] **E3.6** Importador de datos reales con rechazo por lote
  - VERIFICADO: `ImportadorDatosRealesTests` (16 pruebas con libros sintéticos) + ejecución real contra `Base de Datos.xlsx` vía `herramientas/ImportadorCli importar`: **Personal 164/164 OK**, **Personal ausente 12/12 OK**, **Puestos Fijos 98/98 OK** tras la reparación de `SexoPreferente` confirmada por el cliente (00 §G6). El informe de rechazo original era real, no simulado — ver hallazgo e historial abajo

### Decisiones de ingeniería de la etapa E3 (delegadas, no de negocio — R2 no aplica)

- **`Puesto.PerfilRequerido` — columna nueva, no estaba en 04 original.** Al leer el Excel real (hoja "Puestos Fijos") para E3.1 apareció una columna real `PerfilRequerido` (Supervisor/Operador/Averiero/Estibador/Indistinto/Genérico/Operador de filtro) que 00 §A13 ya mencionaba pero que 04 nunca declaró como columna. Es la matriz de compatibilidad dura del §4.2, distinta de `categoria_titular`. Se añade la columna ahora (E3.1) porque el importador (E3.6) necesita dónde escribirla; el motor que la EVALÚA (`fn_CategoriaCompatible`) se construye en **E4**, no aquí — esta etapa solo declara el almacenamiento.
- **`Personal.Sexo` reemplaza `Personal.perfil`.** Mismo hallazgo: el Excel trae `Personal.Sexo` (MASCULINO/FEMENINO) siempre relleno, y A13 ya había cerrado que ese es el otro lado de la comparación con `Puesto.sexo_preferente`. La columna `Perfil` del Excel de Personal es en realidad la **categoría** (mapeada por G1), no una preferencia — nombre engañoso que se documenta explícitamente para que nadie más lo confunda.
- **`Puesto.UmbralCriticoHoras`, no `umbral_critico_min`.** El 04 original tenía los dos umbrales en minutos. A14 (cerrada antes de esta sesión) puebla el "sugerido" desde el dato real en HORAS (`horas_en_puesto`). Comparar horas contra minutos en la misma CHECK sin conversión habría sido un error silencioso; se define el crítico también en horas. Es una decisión técnica de continuidad de A14, no una cifra de negocio — sigue nulable, sin valor inventado.
- **Extensión de G1:** `OPERADOR DE CALDERAS` y `OPERARIO DE FILTROS Y TANQUERIA` (4 personas) no estaban en el resumen original de G1 — solo aparecen leyendo la hoja completa. Mapeadas a Operador A por la misma lógica que `OPERADOR DE EQUIPOS` (equipo específico, no tarea general); el cliente confirmó explícitamente esta parte (*"los de calderas y filtros cuéntalos como operadores"*, 2026-08-09) — queda cerrada para estas 4 personas. `OPERADOR DE EQUIPOS` sigue siendo inferencia propia sin confirmar. Ver `00_DECISIONES.md §G1` actualizado.
- **`rol_app` creado como `USER ... WITHOUT LOGIN`.** Necesario para que el DENY de RestriccionMedica (04 §7.5) sea probable de verdad — sin un principal separado de `dbo`, cualquier prueba de DENY sería un placebo, porque `db_owner` no queda sujeto a un DENY de esta forma. Qué login de servidor se mapea a este rol en cada entorno es una decisión de despliegue (F-block), no de esta migración.
- **El resto del DENY de 04 §7.5** (Asignacion, Movimiento, Auditoria) queda para **E4.7**, tal como decía el plan original — no se adelantó porque esas tablas todavía no tienen flujo de escritura real que proteger.
- **`00_DECISIONES.md §G5` (nueva, abierta, no bloqueante):** los 31 puestos fijos reales solo traen `PerfilRequerido` (Operador/Supervisor/Averiero/Genérico/Operador de filtro), que no mapea limpio a `categoria_titular` (`operador_a`/`operador_c`/`averiero`, 04 §2.6 original). Se relaja `CK_Puesto_categoria` — ya no exige el valor en fijos — en vez de inventar la categoría del titular. Bloquea eventualmente `sp_BarridoPuestosFijos` (E5.5), no la construcción de ahora.
- **Hallazgo real, no de diseño (resuelto el mismo día, ver G6):** al correr el importador contra el archivo real, **Puestos Fijos se rechazó — 9 de 98 filas tenían `SexoPreferente` contaminado con valores de `PerfilRequerido`** (`Femenina` ×1, `Supervisor` ×1, `Operador` ×4, `Averiero` ×1, `Estibador` ×2), tal como A13 ya advertía que podía pasar. El cliente confirmó que `"Femenina"` es tipeo de `"Femenino"`, y que el resto de la Línea 6 no tiene más error de captura del que ya se veía. El importador ahora **repara** esos valores conocidos por coincidencia de patrón (sin una sola excepción en las 91 filas limpias del archivo) en vez de rechazar la fila — `ImportadorDatosReales.ReparacionSexoPreferentePorArrastre`. Reimportado: **los 98 puestos fijos reales cargan limpio**, sin ninguna fila rechazada.
- **`Personal.OrigenDato` / `RestriccionMedica.OrigenDato` — columnas nuevas.** 07 §4.4 exige que "las filas simuladas lleven marca de origen" y que haya una prueba que falle si una llega a producción; no existía dónde guardar esa marca. Valores: `real` (default), `simulado` (persona/restricción inventada solo para probar el mecanismo), `simulado_categoria` (persona **real** del padrón cuya categoría se sobrescribió solo en esta semilla — la re-etiqueta de Operador B/C que pide G1).
- **`Base de Datos.xlsx` salió del control de versiones.** Traía PII real (cédula, INSS, fecha de nacimiento) y había quedado commiteado sin querer en una sesión anterior (`4b7b7e7`). Se agregó a `.gitignore` y se hizo `git rm --cached`; sigue en disco para que el importador lo siga usando. El archivo permanece en el historial de git de este repo local — si algún día se comparte o sube a un remoto, ese commit necesita limpiarse aparte (no basta con esto).
- **`herramientas/ImportadorCli`** — herramienta de línea de comandos nueva, con dos subcomandos (`importar`, `sembrar-adversaria`), nunca invocada desde la Api ni desde una migración. Fija el contexto de sesión como coordinador a mano (la Api real lo hace vía `ContextoSesionMiddleware`, etapa E2) porque estas herramientas no pasan por el pipeline HTTP.
- **Descubrimiento durante las pruebas: la RLS de `Puesto` (etapa E2) también protege las lecturas del propio importador.** Sin contexto de coordinador, el importador no vería sus propias filas al reimportar y produciría un `INSERT` duplicado en vez de una actualización. Se corrigió fijando el contexto antes de cualquier operación — tanto en la herramienta real como en las pruebas (`ComoCoordinadorAsync`).

**Los 16 escenarios de la semilla adversaria (07 §4.4), con su origen:**

| # | Escenario | Decisión |
|---|---|---|
| 1 | Restricción médica vigente que choca con el puesto | §7.2, C14 |
| 2 | Restricción caducada ayer → no bloquea | C14 |
| 3 | Restricción permanente (`fecha_fin` NULL) | C14 |
| 4 | Restricción que empieza mañana → no vigente todavía | C14 |
| 5 | Puesto con fatiga "sugerida" propia (`horas_en_puesto`) | A4, A14 |
| 6 | `umbral_critico_horas` válido, mayor que el sugerido | A4, A14 |
| 7 | "Girar botellas" con 24 h de recuperación | A12 |
| 8 | "Limpieza" con 48 h de recuperación | A12 |
| 9 | Sexo de la persona distinto al preferente del puesto (regla blanda) | A13, §7.3 |
| 10 | Sexo NULL → la regla no se evalúa | A13, §7.3 |
| 11 | Al menos un Operador B por línea activa (L1/L2/L4/L8) | G1 |
| 12 | Al menos un Operador C | G1 |
| 13 | L6 deliberadamente sin ningún Operador B → déficit | G1, prepara C15 |
| 14 | Titular ausente en L1 **con** Operador B disponible en su línea | C1 |
| 15 | Titular ausente en L6 **sin** Operador B en su línea | C1 |
| 16 | L2 exactamente en su piso mínimo; L4 una persona por encima | B5 |

## E4 · Motor de validación *(8/8)* ✅ → F3 · `[BLOQUEANTE]`

- [x] **E4.1** `fn_TieneRestriccionBloqueante` — solo vigentes
  - VERIFICADO: 5 pruebas — vigente bloquea, caducada no, permanente siempre, futura todavía no, sin restricción no bloquea
- [x] **E4.2** `fn_CategoriaCompatible` — matriz §4.2 completa
  - VERIFICADO: 21 casos (`[Theory]`) cubren las cuatro filas de la matriz casilla por casilla, incluidas todas las prohibidas, más el caso G5 (fijo sin `categoria_titular` → nadie compatible)
- [x] **E4.3** `fn_PerfilIncompatible` — regla blanda
  - VERIFICADO: puesto sin preferencia (NULL/Indistinto) no aplica · persona sin sexo registrado no aplica (nunca se infiere) · sexo distinto bloquea · sexo igual no
- [x] **E4.4** `fn_ViolaNoRepeticion24h` — generalizada (A12/B6, ver nota de ingeniería)
  - VERIFICADO: puesto sin horas de recuperación no tiene la regla · misma actividad dentro de la ventana bloquea · misma actividad ya fuera de la ventana (3 jornadas después) no bloquea · actividad distinta no bloquea
- [x] **E4.5** `fn_VentanaArranqueBloquea`
  - VERIFICADO: sin jornada abierta no bloquea · ventana abierta bloquea a quien no está físicamente en la línea · no bloquea a quien sí está · ventana ya cerrada no bloquea a nadie
- [x] **E4.6** `sp_ValidarAsignacion` — los 7 pasos en orden
  - VERIFICADO: 8 pruebas, una por paso — cada escenario hace fallar simultáneamente el paso bajo prueba y uno posterior, y se confirma que el código de rechazo es siempre el del paso más temprano. Confirmado explícitamente: ni `@permitir_ceder_perfil` ni `@es_liderazgo_manual` saltan el paso 4 (médica); `@permitir_ceder_perfil` es el único que cambia el resultado, y solo en el paso 5
- [x] **E4.7** `DENY` sobre tablas críticas
  - VERIFICADO: `INSERT` directo en `Asignacion` con `rol_app` falla con "INSERT permission was denied" (mismo patrón de impersonación `EXECUTE AS` que E3.4). `DELETE`/`UPDATE` en `Auditoria` denegados también. `Movimiento` queda pendiente — esa tabla no existe todavía (ver nota de ingeniería)
- [x] **E4.8** Suite de reglas de seguridad — médicas × 8 caminos
  - VERIFICADO: 8 pruebas — normal, cediendo perfil, liderazgo manual, la función de bajo nivel directamente, vigente hoy, permanente, caducada (control: no debe bloquear), e `INSERT` directo saltándose el procedimiento. Los 8 caminos están definidos y documentados en la nota de ingeniería de abajo — 05 §6.2 dice "médica × 8 caminos" sin enumerarlos

> **→ PC-2** · Validación humana: la restricción médica bloquea por los ocho caminos. **Lista para validar.**

### Decisiones de ingeniería de la etapa E4 (delegadas, no de negocio — R2 no aplica)

- **`fn_ViolaNoRepeticion24h` sintetiza A12 y B6, que nunca se habían puesto una junto a la otra.** A4/B6 (redactadas antes de tener datos reales) describían la regla como "solo Girar botellas, ventana = última jornada trabajada, no calendario". A12 (datos reales, etapa E3) la generalizó a un valor en HORAS propio de cada puesto (`horas_recuperacion`: 24 en Girar botellas, 48 en Limpieza) y jubiló la bandera booleana original. Ninguna decisión revisó la otra. La síntesis usada aquí es literal, no inventada: el ancla temporal es `UltimaTareaJornada.registrado_en` — que por diseño (C13) solo avanza al cerrar un turno realmente trabajado, nunca por el paso del calendario — y el umbral es `Puesto.horas_recuperacion`, el dato real. `docs/06_ROADMAP.md` (P3.4) todavía cita el lenguaje viejo de A4 ("solo la actividad marcada"); queda desactualizado por el mismo motivo que 04 §2.6/§3.1 lo estaban antes de la corrección de la etapa E3.
- **`TipoActividadId` solo se puebla para "Girar botellas".** Es el único agrupamiento que A14 confirma con dato real (las tres filas "Girar botellas 1/2/3" comparten `TiempoDeRecup=24`). Agrupar otros nombres de puesto por similitud de texto (p. ej. "Limpieza") inventaría una equivalencia que el dato no respalda — de hecho lo contradice: "Limpieza" tiene 5h de recuperación en L1 y 48h en L4/L6, tres valores distintos bajo el mismo nombre. El importador (E3.6) se extendió con este único caso; el resto de los 67 puestos rotativos reales queda con `tipo_actividad_id` NULL, lo que es correcto: sin agrupación confirmada, la regla de no repetición simplemente no aplica todavía a esos puestos.
- **`Parametro` se crea en esta etapa** (04 §9), exactamente como se anticipó en la nota de ingeniería de E2 ("candidata natural: E4 o E5"). `fn_VentanaArranqueBloquea` es la primera regla que de verdad necesita un parámetro configurable (`ventana_arranque_min`). Se sigue al pie de la letra `docs/06_ROADMAP.md` P5.1: *"NO inventes umbrales por defecto. Se leen de Parametro y pueden estar vacíos."* No se siembra ninguna fila para `ventana_arranque_min` — sigue "a definir" (04 §9) — y la función trata su ausencia como "la regla no aplica todavía", nunca como un valor por defecto inventado.
- **`JornadaLinea` y `Asignacion` se crean en esta etapa, incompletas a propósito.** `sp_ValidarAsignacion` (04 §7.2) necesita `Asignacion` para su paso 1, y `fn_VentanaArranqueBloquea` necesita saber cuándo cierra la ventana de una jornada — ninguna de las dos tablas existía todavía (el plan las agenda para E5.1/E5.2). `JornadaLinea` se crea aquí con solo las cuatro columnas que E4 necesita (`linea_id`, `arrancado_en`, `ventana_arranque_fin`, `cerrado_en`); el resto del diseño ya completo en 04 §4.1 (`turno_id`, `dia_operacion`, `sku_id`, `supervisor_id`, `estado`) se añade con `ALTER TABLE` cuando `Turno` se construya en E5.1 — mismo patrón ya usado con `Puesto` y `Personal` en E3. `Asignacion` sí se crea completa (04 §5.1 ya estaba totalmente especificado); lo que falta es `sp_AsignarPersona` (la escritura atómica con concurrencia, B1), que queda para cuando exista un endpoint real que la invoque.
- **`JustificacionExcepcion` se crea completa** (04 §5.4) porque `Asignacion.justificacion_id` la referencia por FK — aunque ningún flujo la escribe todavía (eso llega con las excepciones del Coordinador, A6, en etapas posteriores).
- **RLS extendida a `JornadaLinea` (04 §6.3).** La migración `RlsAlcanceLinea` de E2 ya había dejado escrito que el predicado de aislamiento sobre `JornadaLinea` se añadiría "en la migración que cree esa tabla" — es esta. `ALTER SECURITY POLICY ... ADD FILTER PREDICATE ... ON dbo.JornadaLinea`, mismo mecanismo que ya protege `Puesto` desde E2.
- **El DENY de `Movimiento` (04 §7.5) sigue pendiente.** La nota de la etapa E3 anticipaba que E4.7 cubriría "Asignacion, Movimiento, Auditoria" juntas; en la práctica `Movimiento` (Parte X, §5.2) todavía no existe como tabla — se crea en la etapa del motor de movimiento entre líneas (E8). El DENY se aplicará ahí, mismo criterio que ya se usó para no adelantar RLS sobre `JornadaLinea` en E2 cuando esa tabla tampoco existía.
- **Los 8 caminos de la suite médica (E4.8, PC-2) se definieron explícitamente, porque 05 §6.2 exige "médica × 8 caminos" sin enumerarlos.** Elegidos para cubrir literalmente cada cláusula de B12 ("ningún nivel, ningún motor, ningún rol, ninguna urgencia"): (1) camino normal, (2) cediendo perfil preferente, (3) con liderazgo manual (A7b), (4) la función de bajo nivel invocada directamente, (5) restricción vigente hoy, (6) restricción permanente, (7) restricción caducada — control negativo, para probar que la regla no se vuelve más estricta de lo que C14 pide — y (8) `INSERT` directo saltándose `sp_ValidarAsignacion` por completo (mismo mecanismo que E4.7).
- **`@mensaje` de `sp_ValidarAsignacion` es informativo, no la micro-copia literal de 02 §5.4.** El catálogo de 02 §5.4 interpola nombre, línea y minutos restantes — datos de presentación que le corresponde construir a la capa de aplicación cuando exista una UI real que los consuma (etapa E6), no a este procedimiento. Lo que E4.6 verifica y garantiza es `@codigo_rechazo`, que es lo que gobierna el orden y el comportamiento.

## E5 · Jornada, prioridad y barrido *(7/7)* ✅ → F4a

- [x] **E5.1** `Turno` y día de operación · hora del servidor — LEE: `04 §4.1`, `00 §C6`
  - VERIFICADO: `Turno` sembrada vacía (C6 — horarios son dato del cliente), `CruzaMedianoche` computada correcta en ambos casos (normal / cruza medianoche) · `JornadaLinea` completa (`turno_id`, `dia_operacion`, `sku_id`, `supervisor_id`, `estado`, `cerrado_forzado_por`) · `UQ_Jornada(linea_id,turno_id,dia_operacion)` probada
- [x] **E5.2** `JornadaLinea` y `PrioridadLinea` versionada — LEE: `04 §2.2`, `00 §B8`
  - VERIFICADO: `sp_CambiarPrioridadLinea` — intercambia dos líneas sin tocar las otras 8, cierra la fila vigente y abre otra (nunca reescribe el histórico), rechaza orden fuera de 1-10, sin cambio real, y línea sin prioridad vigente
- [x] **E5.3** `SKU`, `PuestoSKU`, cómputo de *fuera de operación* — LEE: `04 §2.5`, `§5.3`
  - VERIFICADO: `fn_PuestoFueraDeOperacion` — 5 casos (sin jornada abierta, jornada sin SKU, sin fila en PuestoSKU nunca fuera de operación, SKU vigente declarado, SKU declarado mas no el vigente) · importador real: `ImportarSkuAsync` (catálogo de 22 SKU reales desde "Programa", todo o nada) + `ImportarPuestosSkuAsync` (9 de 18 filas verificables de "Puestos SKU", 00 §G4 — el resto se omite sin rechazar el lote) · reparación de `categoria_titular` desde `PerfilRequerido` (00 §G5, resuelto)
- [x] **E5.4** Planificación + rechazo si falta supervisor — LEE: `§8.1`, `02 §3.1`
  - VERIFICADO: `sp_PlanificarLinea` (upsert por línea/turno/día, `sku_id=NULL` marca inactiva) + `sp_ConfirmarPlanificacion` — rechazo nominal exacto del ejemplo normativo ("L4, L7"), confirma y sincroniza `Linea.SupervisorActualId` en vivo liberando la línea anterior del supervisor, rechaza confirmar dos veces y replanificar tras confirmar
- [x] **E5.5** `sp_BarridoPuestosFijos` — por prioridad, **solo `tipo='fijo'`** — LEE: `§8.3`, `00 §C12`
  - VERIFICADO: titular presente se autoasigna · titular ausente cae a Operador B conservando su identidad · sin titular ni B → vacante crítica · restricción médica del titular (B12, nunca cede) lo bloquea igual que si estuviera ausente · nunca toca rotativos (C12) ni puestos de Supervisor (§4.1) ni fuera de operación · recorre por prioridad vigente y la línea de mayor prioridad se lleva al único Operador B de toda la planta (A9) · INSERT vía el SP tiene éxito pese al DENY directo sobre `Asignacion` (ownership chaining, E4.7)
- [x] **E5.6** `titular_original_id`, vacante crítica, micro-copia §12.5 — LEE: `§8.3`, `§12.5`
  - VERIFICADO: `titular_original_id` NULL cuando el titular se autoasigna, poblado cuando cubre un suplente · `fn_EsVacanteCritica` (recalculada en vivo, nunca una bandera guardada) · `fn_SituacionPuesto` — las 4 categorías de §5.3 con la precedencia correcta (fuera de operación nunca se confunde con ocupado/libre)
- [x] **E5.7** Ventana de arranque por jornada-línea — LEE: `§8.4`, `02 §4.1`
  - VERIFICADO: `sp_ArrancarTurno` recorre el barrido ANTES de abrir la ventana (02 §3.2, orden exacto del diagrama) · sin `ventana_arranque_min` en `Parametro` la ventana no se activa (mismo criterio que E4.5, R2) · con el parámetro, `ventana_arranque_fin` se calcula con la hora del servidor · arrancar dos veces, sin confirmar, o sin líneas activas se rechaza

### Decisiones de ingeniería de la etapa E5 (delegadas, no de negocio salvo donde se indica)

- **G5 cerrada con el cliente (2026-08-10):** al construir `sp_BarridoPuestosFijos` la falta de `categoria_titular` en los 98 puestos fijos reales (G5, abierta desde E3) pasó de "no bloqueante" a bloquear de verdad — `fn_CategoriaCompatible` (E4.2, ya congelada) rechazaba a cualquiera para esos puestos. Se preguntó y el cliente confirmó: *"por ahora tómalos a todos esos como si fuese operador A, no hay operadores C ni B por los momentos"*. `Averiero` mapea literal a `averiero` (sin ambigüedad); `Operador`/`Genérico`/`Operador de filtro` → `operador_a`; `Supervisor` queda `NULL` a propósito (personal de liderazgo, nunca automático). Ver `00_DECISIONES.md §G5`.
- **`fn_PuestoFueraDeOperacion` — lectura literal de 04 §2.5 corregida.** El texto original decía "fuera de operación si no tiene fila para el SKU vigente", que tomado al pie de la letra habría marcado fuera de operación a TODO puesto sin ningún enlace en `PuestoSKU` — casi todos los 98 fijos reales, que nunca tuvieron esa tabla poblada. Se implementó como gating opt-in: sin ninguna fila, el puesto no depende del SKU (siempre disponible mientras la línea opere); con al menos una fila, solo disponible para los SKU que declara. Decisión técnica de modelado, no de negocio — 04 corregido con la nota.
- **La búsqueda de Operador B suplente en `sp_BarridoPuestosFijos` es de PLANTA, no de línea.** El primer diseño la restringía a `linea_fisica_actual = línea del puesto`, pero eso hacía imposible la contención entre líneas que §8.3 describe explícitamente ("una línea de baja prioridad podría consumir al ÚLTIMO Operador B disponible y dejar sin cubrir un puesto crítico de L4") — con la búsqueda acotada a una sola línea, el orden de prioridad nunca cambiaría nada. Es la asignación INICIAL del día, antes de que exista cualquier tránsito que registrar (Parte X llega en E8), así que no hace falta un movimiento formal para esta asignación inicial.
- **Cada asignación del barrido marca `Personal.situacion = 'asignado'` de inmediato.** Sin esto, el mismo candidato podía "reaparecer" disponible para el siguiente puesto (de la misma línea o de la próxima en el recorrido de `sp_ArrancarTurno`) y el segundo `INSERT` reventaba `UX_Asig_personal_activo` con una excepción cruda en vez de simplemente no encontrar a nadie. Hallazgo real durante las pruebas, no anticipado en el diseño original.
- **`fn_EsVacanteCritica` se recalcula en vivo, nunca se guarda como bandera.** Solo el barrido asigna fijos automáticamente (C12); si un puesto fijo, activo, no fuera de operación, en una línea ya arrancada sigue sin ocupante, es matemáticamente porque el barrido no encontró candidato — no hace falta persistir ese hecho, y recalcularlo es más honesto (§12.4) si algo cambia más tarde.
- **`sp_ArrancarTurno` deja `ventana_arranque_fin` en NULL mientras corre el barrido.** El diagrama del flujo (02 §3.2) pone "arranca la ventana de arranque" DESPUÉS del barrido, no antes — así que la ventana se calcula recién al final, con el mismo instante `@ahora` para las 10 líneas. `fn_VentanaArranqueBloquea` (E4.5) ya trataba NULL como "la regla no aplica todavía", así que ningún candidato del barrido queda bloqueado por una ventana que conceptualmente no ha empezado.
- **`sp_CambiarPrioridadLinea` usa semántica de INTERCAMBIO, no de cascada.** B8 no especifica si mover una línea a un orden ocupado debe desplazar a las demás o intercambiar con quien lo tenía. Se eligió el intercambio (dos filas cambian, nunca las otras ocho) por menor radio de efecto y porque encaja mejor con "quién, cuándo, valor anterior y nuevo" (B8) — decisión de ingeniería, no de negocio.
- **`sp_ConfirmarPlanificacion` sincroniza `Linea.SupervisorActualId`, liberando primero cualquier línea anterior del mismo supervisor.** `JornadaLinea.supervisor_id` (el plan) y `Linea.SupervisorActualId` (la fuente de verdad en vivo del aislamiento, 04 §6.1, D6) son campos distintos a propósito; sin sincronizarlos, confirmar una planificación no le daría al supervisor acceso real a su línea. `UX_Linea_supervisor` exige liberar antes de mover.
- **Puestos fijos con `PerfilRequerido = 'Supervisor'` quedan fuera del barrido, no como vacante crítica.** §4.1 es explícito: "personal de liderazgo nunca se asigna automáticamente por ningún motor". `sp_BarridoPuestosFijos` los excluye de la búsqueda y `fn_EsVacanteCritica` también los excluye — no es un hueco que el motor "falló en resolver".
- **SKU real importado desde la hoja "Programa" (07 §4.1), todo o nada — 22 SKU únicos, deduplicados por `Item`.** `PuestoSKU` real importado desde "Puestos SKU", pero SIN el contrato todo-o-nada del resto de hojas: G4 ya documentaba que esa hoja está deliberadamente incompleta (18 filas, la mayoría sin `Item` verificable). Solo Línea 1 (9 filas, 3 puestos × 3 SKU reales) es completamente verificable contra el catálogo; el resto se omite fila por fila. Un puesto sin ningún SKU verificable NO se crea (se leería como "no depende del SKU", justo lo contrario de lo que dice la hoja de él).
- **`Turno` sigue sembrada vacía en producción** (00 §C6: horarios son dato de configuración del cliente) — la hoja "Programa" trae códigos de turno (`1T8`, `2T8`, `1T12`, `2T12`) pero ningún reloj concreto, y C6 prohíbe inventar una hora. Las pruebas crean sus propios `Turno` de prueba.

## E6 · App base y llenado *(8/8)* → F4b

- [x] **E6.1** Sistema de diseño en Compose — LEE: `03 §2`, `00 §A11`
  - VERIFICADO: tokens de `03 §2` completados sobre lo que ya traía el esqueleto de E0 (`color.bg.overlay`, el par `color.offline` fondo/primer plano, `type.mono`, `elevation.0/1/2/3`) — todos en `Color.kt`/`Type.kt`/`Dimens.kt`, sin ningún componente de `03 §3` adelantado (eso es E6.4/E6.6). 6 pruebas JVM reales nuevas (`testDebugUnitTest`, sin emulador): contraste AAA (7:1) de texto primario/secundario contra las tres superficies — verificado con la fórmula WCAG contra los valores reales, no solo afirmado en un comentario —, los 5 colores de estado de puesto son únicos entre sí, los 3 niveles de fatiga son únicos entre sí, ningún estilo tipográfico baja de 15 sp, y el piso de zona de toque es 48/8 dp (A11). `assembleDebug` sigue en verde
- [x] **E6.2** Los cuatro estados de pantalla — LEE: `03 §3.11`, `§12.4`
  - VERIFICADO: `EstadoPantalla<T>` (sealed interface: `Cargando`/`Listo`/`VacioLegitimo`/`FueraDeOperacion`/`Error`) + `PantallaConEstado` — `when` exhaustivo, sin rama de respaldo tipo spinner genérico. 5 pruebas de Compose **reales, con emulador** (`connectedDebugAndroidTest`, no Robolectric): cargando muestra el esqueleto del llamador y nada más, listo muestra el contenido real, vacío legítimo lleva mensaje + siguiente paso, fuera de operación no se confunde con vacío ni con error, error muestra causa+acción concreta y nunca un código técnico. Primer uso real del emulator `Medium_Phone_API_36.1` de este proyecto
- [x] **E6.3** Login, PIN, alta de dispositivo por QR — LEE: `02 §1.0`, `02 §1.1`, `00 §F3`
  - VERIFICADO: dos endpoints nuevos del catálogo de `05_TRD.md §2.3` que esta UT necesitaba para funcionar de verdad — `GET /api/auth/me` (rol, nombre y línea vigente resuelta en vivo desde `Linea.SupervisorActualId`, nunca del token, §2.3) y `GET /api/servidor/info` (anónimo, para el pre-vuelo del QR) — con 6 pruebas de integración reales (12→18 en `Api.IntegrationTests`). En Android: Hilt + Retrofit/OkHttp (URL del servidor reescrita por interceptor una vez conocida por QR, autorización Bearer que excluye las rutas `AllowAnonymous`) + almacenamiento cifrado por Keystore (`EncryptedSharedPreferences`: device_id, URL, tokens — **la línea nunca se persiste**, §2.3) + lector QR compartido (CameraX + ML Kit *bundled*, el modelo nunca sale del teléfono, 00 §E1/§12.1) + las 4 pantallas del mapa 02 §1.0/§1.1 (`Arranque` reutiliza `EstadoPantalla` de E6.2, `AltaDispositivo`, `Login` con el único mensaje de error sin detalle de cuál credencial falló, `Pin` donde el conteo de intentos vive en el servidor) + enrutamiento puro por rol/línea/L8 (`destinoTrasAutenticar`). **47 pruebas de JVM** (`testDebugUnitTest`) — 14 de ellas **contra la Api real corriendo de verdad** (`dotnet run`, base LocalDB descartable, usuarios con contraseña real sembrados con el nuevo `ImportadorCli crear-usuario-prueba`), el resto sobre los 4 ViewModels con repositorio falso. **13 pruebas de Compose reales con emulador** (5 de E6.2 + 8 nuevas). Backend: 184/184 en verde en todo el backend
- [x] **E6.4** Malla de línea — LEE: `03 §3.1`, `03 §4.3`
  - VERIFICADO: `GET /api/lineas/{lineaId}/puestos` — nuevo, reutiliza `fn_SituacionPuesto` (E5) mapeada como `DbFunction` de EF (nunca reimplementada en C#, para no desincronizarse del motor real de barrido). Devuelve tipo, situación, ocupante, indicador médico (conteo real de `RestriccionMedica` vigentes) y micro-copia — literal de `03 §7.1`/`§7.2` donde el catálogo ya la definía, más una única frase añadida ("Esperando el arranque del turno", para un fijo libre antes de arrancar — no estaba en el catálogo original) con el mismo criterio que `§7.2` ya autoriza. Un rotativo sin ocupante se reclasifica de `libre` a `descubierto` en la capa de presentación — `fn_SituacionPuesto` es del motor de barrido (solo fijos, §5.3), la distinción visual de `§2.1` es de esta UT. 9 pruebas de integración reales — 193/193 en verde en todo el backend. En Android: `TarjetaDePuesto` (franja lateral por estado con ancho distinto por severidad, ocupante, indicador médico, micro-copia — sin barra de fatiga: no existe motor de fatiga todavía, E7, y dibujar una barra sin dato real violaría §1.3) + `MallaLineaScreen` (agrupada fijos→rotativos, fuera de operación colapsado al final, reutiliza `EstadoPantalla` de E6.2 con reintento real a 5 s citado en el propio texto de error). **55 pruebas de JVM en total** (8 nuevas de esta UT: 2 contra la Api real, 6 de `MallaLineaViewModel` con repositorio falso) y **20 de Compose con emulador en total** (7 nuevas) — ambos backends de prueba de Android consolidados en una sola instancia compartida (puerto 5081) para simplificar la infraestructura de aquí en adelante
- [x] **E6.5** Escáner QR con ML Kit en dispositivo — LEE: `00 §E1`, `§12.1`
  - VERIFICADO: nuevo `EscaneoGafeteScreen`/`EscaneoGafeteViewModel` (paquete `ui.escaneogafete`) que envuelve el mismo lector compartido de E6.3 (`EscaneoQrScreen`, CameraX + ML Kit *bundled* — dependencia confirmada `com.google.mlkit:barcode-scanning`, nunca la variante de Play Services que descarga el modelo) para el segundo uso que su propio docstring ya anticipaba: decodificar el número de ficha del gafete (00 §E1), no confirmar identidad (§12.2 es E6.6 — este componente solo decodifica y entrega, nunca asienta nada). 5 pruebas de JVM nuevas (`EscaneoGafeteViewModelTest`) — 60/60 en verde. En Compose: 2 pruebas nuevas, una de ellas **cubre lo que `AltaDispositivoScreenTest` (E6.3) dejó pendiente a propósito** — concede `CAMERA` de verdad con `GrantPermissionRule` (nueva dependencia `androidx.test:rules`) y verifica que la vista de cámara real de `EscaneoQrScreen` se monta sin caerse en el emulador, no solo que cambia un estado. 22/22 en verde con emulador (`Medium_Phone_API_36.1`) — la corrida se restringió a ese dispositivo vía `ANDROID_SERIAL`; un teléfono físico también conectado por USB de una prueba manual anterior tenía la pantalla en reposo (`mWakefulness=Dozing`) y fallaba 21/22 con "No compose hierarchies found", un artefacto del dispositivo (pantalla apagada), no del código — confirmado porque las fallas incluían pruebas de E6.2-E6.4 sin relación con esta UT
- [x] **E6.6** Modal de confirmación de identidad — LEE: `03 §3.3`, `§12.2`
  - VERIFICADO: nuevo `GET /api/personal/por-ficha/{ficha}` (05_TRD.md §2.3, traído antes de su etapa — mismo patrón que E6.3/E6.4/E6.5) resuelve nombre, ficha, categoría y restricciones médicas **vigentes** (mismo filtro `FechaInicio<=hoy && (FechaFin IS NULL OR FechaFin>=hoy)` que el indicador médico de E6.4, aquí listando nombres en vez de solo contar). Deliberadamente **sin alcance de línea** — el gafete puede pertenecer a cualquier persona de la planta (recepciones/relevos, E8/E9); la restricción por línea de §12.2 es de la búsqueda manual (03 §3.5), fuera de esta UT. 6 pruebas de integración nuevas — 199/199 en verde en todo el backend. En Android: `ModalConfirmacionIdentidad` — `Dialog` con `dismissOnBackPress=false`/`dismissOnClickOutside=false` (regla 4 del §3.3: "no responde a un toque accidental de retorno"), reutiliza `EstadoPantalla`/`PantallaConEstado` de E6.2 para cargando/error, bloque médico **nunca colapsable** que afirma la ausencia en vez de omitirla (regla 2, fuente §12.4). El escaneo por sí solo no asienta nada: `onConfirmar` es un callback puro hacia quien invoque el modal — no existe todavía el endpoint que escribe la asignación (llega con E6.7/E6.8). `etiquetaCategoria` extraída de `TarjetaDePuesto` (E6.4) a `ui/comun/EtiquetasDominio.kt`, única fuente para las dos pantallas que la necesitan. 67 pruebas de JVM en total (7 nuevas: 5 de ViewModel con repositorio falso, 2 de integración contra la Api real) y 27 de Compose con emulador en total (5 nuevas)
- [x] **E6.7** `sp_SugerirPuesto` — escalera de 4 niveles — LEE: `§8.5`
  - VERIFICADO: `sp_SugerirPuesto` orquesta piezas que E3/E4 ya habían construido anticipando esta UT, sin reimplementar ninguna regla — `sp_ValidarAsignacion` (E4.6) ya traía `@permitir_ceder_perfil`, exactamente lo que los niveles 2 y 4 necesitan, y los comentarios de `Puesto.TitularId`/`SexoPreferente` (E3) ya documentaban su papel en esta escalera. Las restricciones médicas nunca ceden en ningún nivel — gratis, porque `sp_ValidarAsignacion` no tiene parámetro para saltarse ese paso. Alcance limitado a `tipo='rotativo'` (los fijos ya los resuelve el barrido automático de §8.3 antes de que esta etapa exista — §8.5 vive dentro de "Etapa 4: llenado de rotativos"). Orden determinista por `codigo` entre "cualquier puesto compatible" (decisión de ingeniería, no de negocio: la fuente no fija un orden porque cualquiera es igual de válido). Código nuevo `SIN_PUESTOS_LIBRES` — mismo vocabulario que los siete de `sp_ValidarAsignacion` — para el único caso que ellos no cubren: "nunca silencio" cuando no hay candidato. 11 pruebas nuevas contra la base real — 210/210 en verde en todo el backend. Sin superficie HTTP todavía: el endpoint ya catalogado (`GET /asignaciones/sugerencia?personalId=`, 05_TRD.md §2.3) no tiene todavía quien lo consuma de verdad — llega junto con E6.8, cuando exista el flujo completo sugerir→confirmar→asignar
- [x] **E6.8** Concurrencia: bloqueo determinista + idempotencia — LEE: `04 §7.3`, `00 §B1`
  - VERIFICADO: `sp_AsignarPersona` implementado literalmente sobre el pseudocódigo ya completo de 04 §7.3 (UPDLOCK+HOLDLOCK **puesto antes que persona**, `XACT_ABORT`, tabla `OperacionIdempotente` nueva), con tres adaptaciones documentadas en el comentario de la migración — `sp_RegistrarAuditoria` con su firma real (13 parámetros, no la forma abreviada del snippet), columnas tipadas en vez de `resultado_json`, y rechazo por OUTPUT+COMMIT en vez de `THROW` (mismo estilo que todo el resto del esquema real). Rechazo **nominal** de B1 ("[Nombre] acaba de ser registrado en L4 · Puesto 3 por otro supervisor") construido solo para `PUESTO_OCUPADO`; los rechazos se auditan igual que los éxitos (00 §B12/E2.5), dentro de la misma transacción atómica. 9 pruebas nuevas contra la base real, incluidas dos con conexiones concurrentes de verdad (2 y 10 competidores simultáneos por el mismo puesto — gana exactamente uno en ambas). Traídos también los dos endpoints ya catalogados que le dan un consumidor real a `sp_SugerirPuesto` (E6.7) y `sp_AsignarPersona`: `GET /api/asignaciones/sugerencia` (alcance forzado por servidor, como `/lineas/mi-linea`) y `POST /api/puestos/{id}/asignar` (alcance verificado contra `IAuthorizationService` tras resolver la línea del puesto). 7 pruebas de integración HTTP nuevas — 226/226 en verde en todo el backend.
  - **Bug real encontrado y corregido, no de esta UT en origen**: `RegistradorAuditoria` (E2.5) y mi propio primer intento de `ServicioAsignacion` abrían la conexión con `GetDbConnection().OpenAsync()` directo — eso evita el pipeline de EF y con él `SessionContextConnectionInterceptor` (04 §6.3), dejando `SESSION_CONTEXT` sin fijar y la RLS de `Puesto`/`JornadaLinea` filtrando todas las filas para cualquiera que no fuera coordinador. Invisible en E2.5 porque `Auditoria` no tiene RLS de fila; aquí sí, porque `JornadaLinea` la tiene desde E4.7. Corregido a `db.Database.OpenConnectionAsync()` en ambos sitios.
  - **Sin wiring de Android en esta UT.** El `LEE` de E6.8 es puramente el mecanismo de concurrencia (04 §7.3, 00 §B1) — igual que E6.7 se quedó en el motor sin tocar la app. Conectar de verdad el flujo completo en el teléfono (escanear gafete → confirmar identidad → ver sugerencia → tocar confirmar → que la malla se refresque) es integración adicional, no un UT propio listado en 07_PLAN — hace falta antes de que **PC-3** ("un supervisor llena su línea desde un teléfono real") se pueda validar de punta a punta.
- **Integración de llenado (sin ID propio — no es un UT del plan, es lo que E6.8 dejó pendiente para poder validar PC-3).** Conecta lo que E6.5/E6.6/E6.7/E6.8 ya construyeron y verificaron cada uno por separado, sin reimplementar ninguna regla:
  - `MallaLineaScreen` gana un botón flotante de escanear (`malla-boton-escanear-gafete`) que abre el lector compartido de E6.5. Con la ficha decodificada, arma la malla en un solo recorrido: `FlujoAsignacionViewModel` (nuevo, `ui/asignacion`) resuelve la persona contra `GET /api/personal/por-ficha` (E6.6), pide el destino a `GET /api/asignaciones/sugerencia` (E6.7, `AsignacionRepositorio` nuevo en `data/asignacion`) y muestra `ModalConfirmacionIdentidad` (E6.6, sin modificar) ya con el destino real resuelto. Confirmar llama a `POST /api/puestos/{id}/asignar` (E6.8) con una clave de idempotencia generada una vez por intento de confirmación (00 §B1) y, al cerrar el recorrido —éxito, rechazo o cancelación—, refresca la malla: es la única confirmación visible que necesita el supervisor.
  - El código/tipo del puesto sugerido se resuelve contra la malla que el supervisor ya tiene en pantalla (nunca decide a cuál asignar — esa decisión es enteramente del servidor); si el id sugerido no está en esa lista todavía, se usa un texto de respaldo (`"Puesto {id}"`) que no bloquea nada, porque la asignación real la valida el servidor por id.
  - Los mensajes de rechazo (`SIN_PUESTOS_LIBRES` de E6.7, el rechazo nominal de E6.8 tipo "[Nombre] acaba de ser registrado...") se muestran **tal cual los devuelve el servidor**, nunca reescritos — misma disciplina que el resto de la app (§12.4, R2).
  - 11 pruebas de JVM nuevas (`FlujoAsignacionViewModelTest`) y 5 de Compose con emulador nuevas (1 en `MallaLineaScreenTest`, 4 en `FlujoAsignacionPorFichaTest`) — **78/78 de JVM y 32/32 de Compose en todo Android**, sin tocar ni un archivo de E6.6/E6.7/E6.8 ya verificados.
  - Se recreó a mano el fixture descartable `SmartAssignAndroidJvmTest2` (puerto 5081, ver nota de E6.5 sobre por qué no sobrevive entre sesiones): migraciones al día, los 6 usuarios de prueba y el puesto/persona/restricción de E6.4/E6.6 sembrados de nuevo — las 18 pruebas de integración de JVM que dependen de él (`SesionRepositorioIntegrationTest`, `MallaRepositorioIntegrationTest`, `PersonalRepositorioIntegrationTest`) vuelven a estar en verde.

> **→ PC-3** · **El punto de control más importante.** Un supervisor llena su línea desde un teléfono real. **Listo para validar en un teléfono físico** — el recorrido completo (escanear → confirmar identidad → ver sugerencia → confirmar → malla actualizada) ya existe de punta a punta; falta la validación humana con H4 (gafetes impresos con QR) o un QR de prueba con el número de ficha en texto plano.

### Decisiones de ingeniería de la etapa E6 en curso (delegadas, no de negocio — R2 no aplica)

- **`espresso-core`/`androidx.test.ext:junit` subidos de 3.6.1/1.2.1 a 3.7.0/1.3.0.** El primer intento de correr pruebas reales de Compose contra el emulador (`Medium_Phone_API_36.1`, API 36) falló con `NoSuchMethodException: android.hardware.input.InputManager.getInstance` — Espresso 3.6.1 usa reflexión contra una firma de `InputManager` que la plataforma API 36 ya no tiene. Es una incompatibilidad de la librería de pruebas con la versión de Android del emulador disponible, no del código de la app; las versiones más nuevas del catálogo de Google Maven ya la corrigen.
- **`material-icons-extended` añadida.** Los tratamientos de "vacío legítimo" y "error" (§3.11) exigen icono; el set `material-icons-core` que trae `material3` por defecto no incluye símbolos como "bandeja vacía" o "error". Es una dependencia de compilación normal de AndroidX (sin descarga en tiempo de ejecución) — no viola §12.1.
- **`EstadoPantalla<T>` se construye como sealed interface con 5 casos, no 4.** El brief nombra "los cuatro estados de pantalla" (cargando/vacío/fuera de operación/error) pero ninguna pantalla real puede dibujarse sin un quinto caso para "ya cargó, aquí está el contenido" — se agregó `Listo<T>` junto a los otros cuatro para que el `when` de `PantallaConEstado` sea exhaustivo en el compilador, sin un `else` que pudiera esconder un estado sin tratamiento.
- **Primer uso real de un emulador Android en este proyecto.** E0-E5 solo habían verificado Android con `gradlew assembleDebug` (compila) — nunca con una prueba de Compose corriendo de verdad. Desde E6.2 en adelante, las pruebas de UI corren contra `Medium_Phone_API_36.1` igual que el backend corre contra LocalDB real: nada de Robolectric ni mocks del framework de Compose.
- **`GET /api/auth/me` y `GET /api/servidor/info` traídos antes de su etapa.** Ambos ya estaban en el catálogo de `05_TRD.md §2.3` (`/auth/me`: "Rol, nombre y línea vigente"; `/servidor/info`: "Verificación al escanear el QR de alta"), pero no existían como código — E6.3 los necesitaba para poder decidir la ruta 02 §1.1 y para el paso "¿el servidor responde?" de 02 §1.0. Mismo patrón que E4 trayendo `JornadaLinea`/`Asignacion` antes de su etapa: la Api real ya tenía el hueco documentado, solo faltaba construirlo cuando la primera UT lo necesitó de verdad.
- **`sesión bloqueada por inactividad` (02 §1.1) queda con la rama estructuralmente presente pero nunca activa.** El umbral es `inactividad_bloqueo_sesion_min` (04 §9), declarado "a definir" — exactamente el mismo patrón que `ventana_arranque_min` en E4.5/E5.7. `ArranqueViewModel.sesionBloqueadaPorInactividad()` devuelve `false` con la cita a R2 en el comentario; el nodo del árbol de decisión existe y compila, listo para conectarse el día que el cliente fije el número y exista un endpoint que lo entregue.
- **`ImportadorCli crear-usuario-prueba` — herramienta nueva, dev/test únicamente.** Necesaria para que las 14 pruebas de `SesionRepositorioIntegrationTest` (Android) pudieran autenticarse con una contraseña real (hash+sal reales vía `ServicioCredenciales`, no un usuario simulado) contra la Api real corriendo de verdad. Mismo patrón de guarda que `sembrar-adversaria`: rechaza cualquier cadena de conexión que contenga "Prod" (00 §G2).
- **Primera prueba de JVM de Android contra un backend real, no contra un doble.** `SesionRepositorioIntegrationTest` corre `dotnet run` real (LocalDB descartable, migrada y borrada por la propia sesión de trabajo) y golpea la Api desde un `Retrofit`/`OkHttp` real construido igual que en producción — sin emulador, porque una prueba de JVM corre en el mismo proceso que `localhost`, así que la política `usesCleartextTraffic="false"` de Android (que sí aplica a la app real) no interviene aquí. Las pantallas de Compose, en cambio, se prueban con `FakeSesionRepositorio` (contrato, no red) — la capa que sí vale la pena golpear de verdad es el contrato HTTP, no la lógica de UI.
- **`security-crypto` (`EncryptedSharedPreferences`) para tokens/URL de servidor, no Room/SQLCipher.** Guardar cuatro o cinco valores de sesión no necesita una base de datos — `EncryptedSharedPreferences` (Keystore de Android) es la herramienta correcta para credenciales de sesión. El caché cifrado de datos de negocio con SQLCipher (D3) es un problema distinto, específico de E13.
- **`fn_SituacionPuesto` mapeada como `DbFunction` de EF, no reimplementada en LINQ/C#.** La malla necesita exactamente la misma situación que ya usa `sp_BarridoPuestosFijos` (E5) — escribir la lógica dos veces (una en SQL, otra en C#) es la manera clásica de que las dos empiecen a decir cosas distintas la primera vez que alguien cambie solo una. `SmartAssignDbContext.SituacionPuesto(int)` es un método estático que EF traduce a `dbo.fn_SituacionPuesto(...)` dentro de la consulta — nunca se ejecuta en C#.
- **Un rotativo vacío se muestra `descubierto`, no `libre` — distinción de presentación, no del motor.** `fn_SituacionPuesto` (E5, §5.3) solo conoce cuatro categorías porque el barrido solo toca fijos (C12); el brief de diseño (§2.1) exige un quinto color/forma para "rotativo sin cubrir" porque no es una crisis (nadie lo asigna automáticamente) ni está realmente "libre" (alguien debería estar ahí). El endpoint reescribe `libre`→`descubierto` solo cuando `tipo='rotativo'`, después de leer la función — la función en sí no cambia.
- **Nueva micro-copia "añadida" (§7.2): "Esperando el arranque del turno".** El catálogo literal de §7.1 no contempla un fijo `libre` porque el turno todavía no arrancó — es un estado real (jornada planificada+confirmada, `arrancado_en` aún NULL) que el catálogo original simplemente no anticipó. Redactada en el mismo registro, siguiendo el mismo mecanismo que ya usaron "Rotativo descubierto"/"Puesto reservado"/etc.
- **La barra de fatiga (anatomía #6 de `03 §3.1`) no se construye en esta UT.** No existe ningún motor de fatiga todavía (E7, `0/4`) — dibujar una barra sin un número real detrás sería mentirle al supervisor (§1.3, principio 1). La tarjeta ya tiene el resto de su anatomía completa; la barra se añade cuando E7 exista un `%` real que mostrar.
- **`ImportadorCli crear-usuario-prueba` corregido: libera la línea previa antes de asignar la nueva.** Al re-sembrar un mismo usuario de prueba en otra línea (E6.4 reutilizando usuarios de E6.3), la herramienta violaba `UX_Linea_supervisor` por no soltar la línea anterior primero — mismo defecto que `sp_ConfirmarPlanificacion` (E5.4) ya había resuelto correctamente en el motor real. Corregido con el mismo criterio: liberar, luego asignar.
- **Los dos backends de prueba de JVM de Android (E6.3 y E6.4) se consolidaron en una sola instancia compartida, puerto 5081.** Tenerlos por separado (5080 para sesión, 5081 para malla) exigía recordar arrancar dos procesos y dos bases descartables cada vez que corrieran las 55 pruebas de JVM juntas — un solo servidor, sembrado una vez con todos los usuarios y el puesto de prueba que cada suite necesita, es más simple y es lo que de verdad se usará de aquí en adelante.
- **Dos defectos de E6.3 encontrados en la primera prueba de campo con teléfono físico real (H2), corregidos aquí.** (1) `android:usesCleartextTraffic="false"` en el manifiesto bloqueaba **toda** petición HTTP de la app —incluida `http://<ip-de-planta>/api/servidor/info`—, así que ningún dispositivo físico real pudo haber completado nunca la alta por QR con esa configuración; la arquitectura entera asume HTTP plano en la red interna de planta (nunca HTTPS, 00 §F1), así que se corrige a `true`. (2) `launchSettings.json` fijaba `http://localhost:5099`: Kestrel solo aceptaba conexiones del propio equipo, nunca de un teléfono en la misma Wi-Fi — cambiado a `http://0.0.0.0:5099`. Ninguno de los dos lo había expuesto una prueba de JVM ni de Compose con emulador, porque ambas corren en el mismo proceso/red que el servidor; hacía falta el teléfono físico de H2 para que apareciera.
- **El fixture descartable de pruebas de Android (`SmartAssignAndroidJvmTest2`, puerto 5081) no sobrevive entre sesiones y hay que recrearlo a mano.** Las migraciones quedaron aplicadas de una sesión anterior, pero ni el proceso `dotnet run` ni los 6 usuarios (`coord_android`, `sup_l4_android`, `sup_l8_android`, `sup_sinlinea_android`, `sup_l4_malla`, `sup_l1_malla`) ni el puesto `L4-JVM01` (línea 9) persisten. Además, `SmartAssignDbContextFactory` (diseño) apunta siempre a `SmartAssignDev` — para migrar una base distinta hace falta `dotnet ef database update --connection "<otra-cadena>"`, la variable de entorno `ConnectionStrings__SmartAssignDb` no basta en tiempo de diseño (sí en tiempo de ejecución real, vía `dotnet run`). Sigue sin existir un script que automatice esto — sería un candidato razonable para una UT de infraestructura de pruebas si el costo de repetirlo a mano sigue creciendo.
- **E6.6 añadió al mismo fixture** una persona (`Personal.ficha = 'F-JVM01'`, categoría `operario`) con una restricción médica permanente y vigente sobre una `CapacidadFisica` nueva (`codigo = 'CAP-JVM01'`, nombre "No levantar carga superior a 10 kg") — igual de manual, igual de no persistente entre sesiones, mismo comentario de arriba aplica.

## E7 · Fatiga *(4/4)* ✅ → F5

- [x] **E7.1** Reloj desde `Asignacion.inicio` con umbral propio — LEE: `§9.1`, `00 §A4`
  - VERIFICADO: `fn_MinutosEnPuesto` — minutos desde `Asignacion.inicio` del ocupante activo (`fin IS NULL`), `NULL` (nunca 0) cuando la fatiga no aplica: puesto fijo (§9.1, "los operadores en puestos fijos no entran en este cálculo"), inactivo, o sin nadie asignado. `fn_UmbralFatigaSugeridoMinutos`/`fn_UmbralFatigaCriticoMinutos` resuelven el "umbral propio" (00 §A4): primero `Puesto.horas_en_puesto`/`umbral_critico_horas` (convertidos a minutos), si el puesto no lo trae cae a `Parametro['fatiga_sugerido_default_min']`/`['fatiga_critico_default_min']` (mismo nombre ya catalogado en 04 §9), sin ninguno de los dos `NULL` — la regla no aplica todavía, ningún umbral inventado (R2), mismo criterio que `fn_VentanaArranqueBloquea` (E4.5) con `ventana_arranque_min`. Deliberadamente sin clasificar en niveles ni calcular exceso relativo — eso es E7.2/E7.3. 9 pruebas nuevas — 235/235 en verde en todo el backend. **Bug de prueba encontrado y corregido, mismo defecto real que E6.8**: el primer intento de las pruebas abría la conexión cruda sin fijar `SESSION_CONTEXT`, dejando `Puesto` invisible por RLS y las comparaciones `IF @tipo <> 'rotativo'` en `UNKNOWN` (ni verdadero ni falso) en vez de bloquear — no es un defecto de las funciones, es el mismo patrón de contexto de coordinador que ya usa el resto de la suite
- [x] **E7.2** Exceso relativo en % — LEE: `00 §A4`, `00 §B3`
  - VERIFICADO: `fn_ExcesoRelativoFatiga` = `minutos_en_puesto / umbral_sugerido_minutos * 100`, `DECIMAL(6,2)` (forma ya catalogada en 04 §9). Denominador siempre el umbral **sugerido** — es el punto de referencia único para el avance continuo (03 §2: "la barra se llena progresivamente desde el minuto cero", "porcentaje sobre el umbral propio del puesto"); el crítico se compara aparte, en minutos absolutos, cuando exista la clasificación de niveles (E7.3). `NULL` (nunca un porcentaje sin denominador real, R2) cuando `fn_MinutosEnPuesto` o `fn_UmbralFatigaSugeridoMinutos` (E7.1) ya son `NULL`. Prueba dedicada del ejemplo normativo exacto de 00 §A4: 70 min sobre umbral 60 (116.67%) es peor que 70 min sobre umbral 120 (58.33%). 7 pruebas nuevas — 242/242 en verde en todo el backend
- [x] **E7.3** Tres niveles · fijos no acumulan · fatiga del puesto — LEE: `§9.1`, `§5.1`, `00 §A7`
  - VERIFICADO: `fn_NivelFatiga` — `normal`/`sugerido`/`critico`, reutilizando `fn_MinutosEnPuesto`/`fn_UmbralFatigaSugeridoMinutos`/`fn_UmbralFatigaCriticoMinutos` (E7.1) sin reimplementar la resolución de umbral propio-o-planta. Crítico se evalúa antes que sugerido (por encima de ambos umbrales es crítico, nunca se queda en sugerido); exactamente en el umbral ya cuenta como alcanzado. `normal` solo se afirma cuando hay al menos un umbral real con qué comparar — sin ninguno de los dos, `NULL` (nada calibrado todavía, mismo criterio que E7.1/E7.2, R2). "Los operadores en puestos fijos no entran en este cálculo" (§9.1/§5.1) y "la fatiga es propiedad del puesto, no de la categoría" (00 §A7) llegan **gratis por construcción** — `fn_MinutosEnPuesto` nunca lee `Personal.categoria` — y quedan verificados con una prueba `[Theory]` (operario/operador_b/operador_c ocupando el mismo tipo de puesto, mismo nivel). 10 pruebas nuevas — 252/252 en verde en todo el backend. Ajuste de estabilidad en `ExcesoRelativoFatigaTests` (E7.2): la tolerancia de `0.01` en los porcentajes resultó demasiado ajustada corriendo la suite completa en paralelo (contención real de LocalDB entre el instante en que `Inicio` se marca en C# y el instante en que la función lee `SYSUTCDATETIME()`) — ampliada a `6` puntos porcentuales, mismo criterio de tolerancia que ya usaba `RelojDeFatigaTests` con minutos
- [x] **E7.4** Factor de doble turno (default 1.0) + barra continua — LEE: `00 §B7`, `§11.5`
  - VERIFICADO: **E7 completa (4/4).** `fn_MinutosEnPuestoEfectivos` — nueva, NO reemplaza `fn_MinutosEnPuesto` (E7.1): 01_PRD.md HU-D2 muestra el aviso a otros supervisores con el minuto *literal* ("L4 · Puesto 3 — relevo sugerido · 62 min"), así que el reloj crudo debe seguir siendo el real, sin ajustar. El factor de doble turno (`Personal.doble_turno` × `Parametro['factor_doble_turno']`, default **1.0 — el único caso de todo el motor de fatiga donde 00 §B7 sí especifica el número por defecto**, no "a definir" como el resto) solo alcanza la CLASIFICACIÓN y el ORDENAMIENTO: `fn_ExcesoRelativoFatiga`/`fn_NivelFatiga` (E7.2/E7.3) se alteran (`CREATE OR ALTER`, mismo contrato de salida) para leer el reloj efectivo — ningún llamador externo cambia. `Puesto.doble_turno` es "distintivo visual permanente, en toda pantalla donde aparezca" — ya existía desde E3, esta UT solo lo expone. 16 pruebas nuevas de backend (6 `FactorDobleTurnoTests` + 2 de integración HTTP + confirmación de que E7.2/E7.3 siguen en verde con la función alterada) — **260/260 en verde en todo el backend**. En Android: la malla (`GET /api/lineas/{lineaId}/puestos`, E6.4) ahora trae `nivelFatiga`/`excesoFatiga`/`ocupante.dobleTurno`, mapeados como `DbFunction` (mismo patrón que `fn_SituacionPuesto`). `TarjetaDePuesto` dibuja la barra de fatiga que E6.4 había dejado pendiente explícitamente ("no existe motor de fatiga todavía") — grosor y color por nivel (03 §2: fina/media+reloj/gruesa+alerta+pulso lento), `NULL` nunca dibuja nada (§1.3) — y el distintivo de doble turno junto al nombre del ocupante. 6 pruebas de Compose nuevas — 38/38 en verde con emulador. Decisión de alcance: no se extendió el contrato JVM-contra-servidor-real (`MallaRepositorioIntegrationTest`) con los campos nuevos — ya está probado de punta a punta en el backend (HTTP real) y es el mismo patrón mecánico de paso de campos que el resto del DTO, nunca antes verificado por separado tampoco

## E8 · Movimiento entre líneas *(3/6)* → F6

- [x] **E8.1** Despacho con `hora_salida` — LEE: `Parte X`, `§12.7`, `04 §5.2`
  - VERIFICADO: tabla `Movimiento` creada completa de una vez (04 §5.2 ya estaba totalmente especificada — mismo criterio que `Asignacion` en E4.1), incluida `duracion_seg` como columna calculada persistida (`DATEDIFF(SECOND, hora_salida, hora_llegada)`, la razón de ser de §12.7 según su propio texto), `UX_Mov_transito`/`UX_Mov_reserva`/`IX_Mov_analitica` y el `DENY INSERT/UPDATE/DELETE` sobre `rol_app` que la nota de ingeniería de E4.7 ya había anticipado para esta etapa. `sp_DespacharPersona` implementa literalmente el paso 1 de Parte X: resuelve `linea_origen` del propio padrón (`Personal.linea_fisica_actual`, nunca de un valor que mande quien llama), rechaza si la persona no está físicamente en ninguna línea (`SIN_LINEA_FISICA`) o si no está disponible — "en Bolsón o sin asignar", literal — (`NO_DISPONIBLE_PARA_DESPACHO`), y bajo `UPDLOCK`+`HOLDLOCK` sobre `Personal` (mismo criterio de bloqueo determinista que B1) rechaza un segundo despacho mientras el primero sigue en tránsito (`YA_EN_TRANSITO`). Dos supervisores despachando a la misma persona a la vez: gana exactamente uno (prueba con 5 conexiones concurrentes reales). Deliberadamente **sin** la inmunidad real durante el tránsito en otros motores (E8.2) ni recepción/rechazo/reserva (E8.3-E8.5) — esta UT solo abre la fila y dice `situacion='en_transito'`. 12 pruebas nuevas — 272/272 en verde en todo el backend
- [x] **E8.2** Tránsito inmune — LEE: `§6.1`, `04 §5.2`
  - VERIFICADO: §6.1, literal — "mientras alguien camina entre dos puntos, ninguna otra terminal puede capturarlo ni reasignarlo". No hizo falta mecanismo nuevo: las dos piezas que ya otorgaban la inmunidad existían desde antes de que `Movimiento` existiera — `sp_ValidarAsignacion` paso 2 (E4.6) ya rechazaba cualquier `situacion` distinta de `presente_sin_asignar`/`en_bolson` con `PERSONA_NO_DISPONIBLE`, y `UX_Mov_transito` (E8.1) ya era la defensa de última línea a nivel de base de datos. Esta UT prueba, con el camino real de punta a punta (despachar de verdad vía `sp_DespacharPersona`, nunca sembrando `situacion='en_transito'` a mano), que la garantía se sostiene en los dos caminos de captura que ya existen: `sp_AsignarPersona` directo (rechazada tanto en una línea ajena como en su propia línea de destino — "su destino ya está comprometido") y la escalera de 4 niveles `sp_SugerirPuesto` (E6.7, nunca ofrece a quien está en tránsito aunque haya puesto libre compatible). Un cuarto caso prueba `UX_Mov_transito` directamente a nivel de SQL: dos `INSERT` crudos con `estado='en_transito'` para la misma persona (bypaseando el SP) violan el índice único. De paso, corrigió un test flaky preexistente de E8.1 (`Dos_supervisores_despachando...`): el chequeo previo de `situacion` en `sp_DespacharPersona` vive fuera de la transacción, así que bajo una carrera real los 4 perdedores pueden repartirse de forma no determinista entre `YA_EN_TRANSITO` y `NO_DISPONIBLE_PARA_DESPACHO` según timing de máquina — la aserción exigía una distribución fija (4/0) que dos corridas aisladas ya contradecían (halladas 0 y 1); ahora verifica la invariante real (exactamente 1 gana, los otros 4 caen en uno de los dos códigos válidos). 4 pruebas nuevas — 276/276 en verde en todo el backend
- [x] **E8.3** Recepción individual con `hora_llegada` — LEE: `00 §C8`, `03 §4.5`
  - VERIFICADO: `sp_RecibirPersona` implementa literalmente el paso 3 de Parte X — "el supervisor destino confirma que llegó físicamente, **y solo entonces** se le asigna el puesto": el "y solo entonces" es la clave, este SP NO asigna ningún puesto, solo cierra el tránsito (`estado='recibido'`, `hora_llegada` real, `recibido_por`) y deja a la persona disponible para el flujo de asignación ya existente (E4.6/E6.7/E6.8). Sin cambios de esquema — `Movimiento` ya traía cada columna necesaria desde E8.1. 00 §C8 "proporcionalidad de la verificación": recibir no exige categoría ni restricciones médicas (eso se valida al asignar); confirmación individual por `@movimiento_id`, nunca en bloque. El destino decide la situación resultante usando `Linea.EsBolson` — nunca asumiendo que "línea 8" es el Bolsón por convención (comentario propio de la entidad, 04 §2.1): `en_bolson` si el destino lo es, si no `presente_sin_asignar`; `linea_fisica_actual` se actualiza recién aquí (en el despacho, E8.1, la persona todavía no había llegado). Bloqueo determinista bajo `UPDLOCK`+`HOLDLOCK` desde el propio SELECT inicial (todo el chequeo vive dentro de la transacción, a diferencia del pre-chequeo de `sp_DespacharPersona`) — por eso la prueba de 5 confirmaciones concurrentes reales es determinista: exactamente 1 gana, las otras 4 caen siempre en `MOVIMIENTO_NO_EN_TRANSITO`, sin la variabilidad que tuvo que tolerarse en E8.2. 7 pruebas nuevas — 283/283 en verde en todo el backend
- [ ] **E8.4** Rechazo con motivo obligatorio — LEE: `00 §C10`
- [ ] **E8.5** Reserva de puesto sin convergencia — LEE: `00 §B4`, `04 §5.2`
- [ ] **E8.6** `sp_CaducarTransitos` — alerta sin mover a nadie — LEE: `00 §B11`

## E9 · Motor de relevos *(0/9)* → F7

- [ ] **E9.1** `SolicitudRelevo` · el puesto no se libera al avisar — LEE: `§9.4 p1`, `04 §5.3`
- [ ] **E9.2** Orden de cola: crítico → exceso relativo → FIFO — LEE: `00 §B3`
- [ ] **E9.3** Aviso a todos los supervisores **sin identidad** — LEE: `00 §D2`, `§2.2`
- [ ] **E9.4** `vw_SolicitudRelevo_L8` — proyección mínima — LEE: `00 §D1`, `04 §6.3`
- [ ] **E9.5** `sp_ProponerRelevista` — ranking exacto — LEE: `00 §B2`
- [ ] **E9.6** Aceptar/rechazar + descartados con caducidad — LEE: `§9.4 p3`, `00 §B10`
- [ ] **E9.7** `sp_SugerirDestinoRelevado` — línea → proximidad → L8 — LEE: `00 §B4`, `00 §A1`, `00 §A9`
- [ ] **E9.8** Prueba del ejemplo normativo del §9.4 — LEE: `§9.4 ejemplo`, `00 §A8`
- [ ] **E9.9** Prueba de arquitectura: Relevos no referencia prioridad — LEE: `00 §A9`, `05 §4.1`

> **→ PC-4** · El ejemplo del §9.4 completo, con relevo en cadena.

## E10 · Extracción inversa y vacante crítica *(0/6)* → F8

- [ ] **E10.1** Orden derivado invirtiendo la prioridad — LEE: `00 §A5`, `§9.6`
- [ ] **E10.2** Solo con la L8 completamente vacía — LEE: `§9.6`
- [ ] **E10.3** Piso por línea · inmunidad — LEE: `00 §B5`
- [ ] **E10.4** Escalera C15 N1→N4 + guarda anti-dominó — LEE: `00 §C15`
- [ ] **E10.5** `JustificacionExcepcion` en toda excepción — LEE: `00 §A6`, `04 §5.4`
- [ ] **E10.6** Titular reincorporado + salida de retiro temporal — LEE: `00 §C1`, `00 §C2`

> ⚠ **Bloqueada hasta H7:** cerrar `A5b` (si L4 puede ser donante) y `A7-orig` (liderazgo y matriz de categoría).

## E11 · Contingencias y estadística *(0/8)* → F9

- [ ] **E11.1** Paro: dos niveles + descripción obligatoria — LEE: `§11.1`, `04 §4.3`
- [ ] **E11.2** Fijos ocupados · rotativos liberados con tránsito individual — LEE: `§11.1`, `00 §C8`
- [ ] **E11.3** Cronómetro persistente en todas las pantallas — LEE: `§11.1`, `03 §3.8`
- [ ] **E11.4** Relevista en tránsito hacia línea en paro — LEE: `00 §C9`
- [ ] **E11.5** `Lote` y cambio de SKU — LEE: `00 §C5`, `§11.2`
- [ ] **E11.6** Desperdicio + producción + justificación sobre umbral — LEE: `§11.3`, `00 §C4`
- [ ] **E11.7** Eficiencia calculada en el servidor — LEE: `§11.4`, `00 §C4`
- [ ] **E11.8** Todo registro empuja a los dos paneles — LEE: `00 §C4`

## E12 · Tiempo real y notificaciones *(0/6)* → F10

- [ ] **E12.1** Hub SignalR con grupos asignados por el servidor — LEE: `05 §2.4`, `§2.2`
- [ ] **E12.2** Catálogo de eventos — LEE: `05 §2.4`
- [ ] **E12.3** Bandeja de salida transaccional — LEE: `05 §4.1`
- [ ] **E12.4** FCM campana vacía + descarga del contenido real — LEE: `00 §D5`, `05 §2.5`
- [ ] **E12.5** Prueba que falla si la carga útil lleva negocio — LEE: `00 §D5`, `§12.1`
- [ ] **E12.6** Acuse, escalado y *"supervisor no localizable"* — LEE: `00 §D5`, `04 §10`

## E13 · Modo sin conexión *(0/5)* → F11

- [ ] **E13.1** Room + SQLCipher con clave en Keystore — LEE: `00 §D3`
- [ ] **E13.2** Caché acotada a su línea — LEE: `00 §D3`
- [ ] **E13.3** Bloqueo defensivo · **no se encola nada** — LEE: `§12.1`, `05 §4.3`
- [ ] **E13.4** Sello de frescura + degradación visual — LEE: `00 §D4`, `03 §3.7`
- [ ] **E13.5** Detección por latido — LEE: `05 §4.3`

> **→ PC-5** · Se corta la red y nada queda encolado.

## E14 · Cierre, histórico y endurecimiento *(0/7)* → F12

- [ ] **E14.1** Cierre de turno con lista exacta de bloqueos — LEE: `00 §C13`, `02 §4.10`
- [ ] **E14.2** `UltimaTareaJornada` + cierre forzado con justificación — LEE: `00 §B6`, `00 §A6`
- [ ] **E14.3** Histórico y auditoría consultable — LEE: `§2.1.11`, `§12.7`
- [ ] **E14.4** Rendimiento contra presupuestos — LEE: `05 §3.4`
- [ ] **E14.5** Accesibilidad: 48 dp, AAA, escala de grises — LEE: `03 §5`, `00 §A11`
- [ ] **E14.6** Distribución del APK + verificación de versión — LEE: `00 §F3`, `04 §10.1`
- [ ] **E14.7** Carga de datos reales + purga de lo simulado — LEE: `07 §4.3`

> **→ PC-6** · Piloto: un turno real completo, sin papel.

---

## Bloqueos activos

| ID | Qué falta | Bloquea |
|---|---|---|
| **H7** | Cerrar `A5b` y `A7-orig` | E10 |
| **H4** | Gafetes impresos con QR | Pruebas de campo desde E6 |
| **H6** | Vocabulario real de capacidades físicas | Producción, no construcción |
| **H2** | Teléfonos físicos | PC-1 en adelante |

> **H8 resuelto (2026-08-09, mismo día que se abrió):** el cliente confirmó que `"Femenina"` es error de tipeo de `"Femenino"`, y que el resto de la tabla (incluida la Línea 6) describe puestos y perfil técnico reales. Ver `00_DECISIONES.md §G6`. Los 98 puestos fijos reales ya importan sin errores.

## Registro de sesiones

| Fecha | UTs completadas | Notas |
|---|---|---|
| 2026-08-09 | E0 (3) + E1 (6) | Esqueletos backend/Android, migración base, semilla estructural con corrección A1 verificada |
| 2026-08-09 | E2 (6) | Identidad y aislamiento en tres capas (JWT sin `linea_id`, filtro de alcance, RLS). 32/32 pruebas en verde. Deuda documentada: bloqueo automático por reintentos sin umbral (falta `Parametro`, creada en E4), RLS de `JornadaLinea` diferida a cuando esa tabla exista (llegó en E4, no E5 como se estimaba aquí) |
| 2026-08-09 | E3.1–E3.4 (4) | `Personal`, extensión de `Puesto` (titular, umbrales en horas, `PerfilRequerido`), `PuestoCapacidad`, `RestriccionMedica` con `DENY DELETE` real vía `rol_app`. 38/38 pruebas en verde. Corrige 04 en dos puntos que A13/A14 ya habían cerrado pero el documento no reflejaba (`Personal.Sexo`, `Puesto.PerfilRequerido`). Extiende G1 con 4 personas no cubiertas por el resumen original. |
| 2026-08-09 | E3.5–E3.6 (2) | **E3 completa.** Importador real ejecutado contra `Base de Datos.xlsx`: Personal 164/164 y Ausencias 12/12 cargados; Puestos Fijos rechazado por 9 filas con dato contaminado (00 §A13, confirmado con datos reales, no solo predicho) — nuevo bloqueo H8, no crítico. Semilla adversaria de los 16 escenarios (07 §4.4) construida y corrida contra `SmartAssignDev`. Nueva decisión G5 (categoría de titular de puesto fijo no derivable). `Base de Datos.xlsx` sacado del control de versiones (traía PII real). 67/67 pruebas en verde en todo el backend |
| 2026-08-09 | H8 resuelto | Cliente confirmó `Femenina`=`Femenino` (tipeo) y que la Línea 6 no tiene más error de captura que el ya visto; calderas/filtros confirmados como Operador A. Nueva decisión G6: el importador repara `SexoPreferente` arrastrado por patrón en vez de rechazar. Reimportado: **98/98 Puestos Fijos reales cargan limpio**. 7 pruebas nuevas/ajustadas — 74/74 pruebas en verde en todo el backend |
| 2026-08-09 | E4 (8) | **Motor de validación completo — PC-2 lista para validar.** Las 5 funciones de §7.1 + `sp_ValidarAsignacion` con los 7 pasos exactos + DENY sobre `Asignacion`/`Auditoria` + suite médica de 8 caminos. Síntesis de ingeniería entre A12 y B6 para `fn_ViolaNoRepeticion24h` (ver nota abajo). Tablas nuevas: `Parametro`, `JustificacionExcepcion`, `JornadaLinea` (mínima), `Asignacion`, `UltimaTareaJornada` — todas ya especificadas en 04, traídas antes de su etapa porque E4 las necesita para compilar/probar. RLS extendida a `JornadaLinea` (04 §6.3, ya anticipada desde E2). 58 pruebas nuevas — 132/132 en verde en todo el backend |
| 2026-08-10 | E5 (7) | **Jornada, prioridad y barrido completo.** `Turno` (vacía, C6) + `JornadaLinea` completa · `sp_CambiarPrioridadLinea` (intercambio, B8) · `fn_PuestoFueraDeOperacion`/`fn_EsVacanteCritica`/`fn_SituacionPuesto` (§5.3) · `sp_PlanificarLinea`/`sp_ConfirmarPlanificacion` (rechazo nominal si falta supervisor, sincroniza `Linea.SupervisorActualId`) · `sp_BarridoPuestosFijos` + `sp_ArrancarTurno` (§8.3/§8.4, barrido ANTES de la ventana). G5 cerrada con el cliente a mitad de la etapa (categoria_titular). Importador real extendido: SKU (22, todo o nada) + Puestos SKU (9/18 verificables, 00 §G4). 46 pruebas nuevas — 178/178 en verde en todo el backend |
| 2026-08-10 | E6.1 (1) | **Arranca E6, primer UT de Android desde E0.** Sistema de diseño de `03 §2` completado sobre el esqueleto de tokens que ya traía E0: `bg.overlay`, par `offline` fg/bg, `type.mono`, `elevation.0-3`. 6 pruebas JVM reales (`testDebugUnitTest`, sin emulador) verifican contraste AAA real (fórmula WCAG, no solo el comentario), unicidad de colores de estado/fatiga, piso tipográfico de 15 sp y zonas de toque de A11. `assembleDebug` sigue en verde |
| 2026-08-10 | E6.2 (1) | **Primeras pruebas de Compose con emulador real** (`Medium_Phone_API_36.1`). `EstadoPantalla<T>` (5 casos: `Cargando`/`Listo`/`VacioLegitimo`/`FueraDeOperacion`/`Error`) + `PantallaConEstado`, `when` exhaustivo. 5/5 pruebas de Compose reales en verde tras subir `espresso-core`/`androidx.test.ext:junit` (incompatibilidad de reflexión de `InputManager` con API 36 en la versión anterior). `material-icons-extended` añadida |
| 2026-08-10 | E6.3 (1) | **Login, PIN y alta de dispositivo por QR.** Backend: `GET /api/auth/me` + `GET /api/servidor/info` (ya estaban en el catálogo de 05_TRD, traídos antes de su etapa) — 184/184 en todo el backend. Android: Hilt + Retrofit/OkHttp + `EncryptedSharedPreferences` + lector QR compartido (CameraX + ML Kit *bundled*) + las 4 pantallas del mapa 02 §1.0/§1.1 + enrutamiento puro por rol/línea/L8. 47 pruebas de JVM (14 contra la Api real corriendo de verdad, con usuarios de contraseña real sembrados por el nuevo `ImportadorCli crear-usuario-prueba`) + 13 de Compose con emulador. Primer establecimiento del patrón "JVM contra backend real" para Android |
| 2026-08-10 | E6.4 (1) | **Malla de línea — el componente central de la app.** `GET /api/lineas/{lineaId}/puestos` reutiliza `fn_SituacionPuesto` (E5) vía `DbFunction` de EF, nunca reimplementada — 193/193 en todo el backend (9 pruebas nuevas). `TarjetaDePuesto` (03 §3.1, sin barra de fatiga: E7 no existe) + `MallaLineaScreen` agrupada fijos→rotativos con fuera-de-operación colapsado (03 §4.3). Rotativo vacío se muestra `descubierto`, no `libre` (distinción de presentación sobre el mismo dato). Nueva micro-copia "añadida" (§7.2): "Esperando el arranque del turno". 55 pruebas de JVM y 20 de Compose en total (8 y 7 nuevas). Los dos backends de prueba de Android (E6.3/E6.4) se consolidaron en un solo servidor compartido |
| 2026-08-10 | E6.5 (1) | **Escáner de gafete — segundo uso del lector compartido de E6.3.** `EscaneoGafeteScreen`/`EscaneoGafeteViewModel` decodifican el número de ficha (00 §E1) reutilizando `EscaneoQrScreen` (CameraX + ML Kit *bundled* confirmado, §12.1) sin confirmar identidad (§12.2 queda para E6.6 a propósito). Nueva prueba de Compose con `GrantPermissionRule` concede `CAMERA` de verdad y prueba la vista de cámara real — la cobertura que `AltaDispositivoScreenTest` (E6.3) había dejado pendiente explícitamente para esta UT. Restaurado además el fixture descartable de pruebas de Android (`SmartAssignAndroidJvmTest2`, puerto 5081): estaba con las migraciones al día pero sin el servidor arriba ni los 6 usuarios/1 puesto que las suites de E6.3/E6.4 esperan — recreado con `ImportadorCli crear-usuario-prueba` y una siembra directa de `Puesto` bajo el contexto RLS de coordinador. 60/60 pruebas de JVM y 22/22 de Compose con emulador en verde (5 y 2 nuevas) |
| 2026-08-10 | E6.6 (1) | **Modal de confirmación de identidad — el componente de seguridad del §3.3.** Nuevo `GET /api/personal/por-ficha/{ficha}` (traído antes de su etapa, mismo patrón que toda la etapa E6) resuelve nombre/ficha/categoría/restricciones vigentes, deliberadamente sin alcance de línea (el gafete puede ser de cualquiera de la planta). 199/199 en verde en todo el backend. `ModalConfirmacionIdentidad` en Android: `Dialog` que no cede al botón de retorno (regla 4 del §3.3), bloque médico nunca colapsable con ausencia afirmada explícitamente (regla 2, fuente §12.4), reutiliza `EstadoPantalla`/`PantallaConEstado` de E6.2. `onConfirmar` es un callback puro — consolidar la asignación de verdad no existe todavía como endpoint, llega con E6.7/E6.8. `etiquetaCategoria` extraída de `TarjetaDePuesto` a un util compartido para no arriesgar que las dos pantallas la etiqueten distinto. 67 pruebas de JVM y 27 de Compose con emulador en total (7 y 5 nuevas) |
| 2026-08-10 | E6.7 (1) | **`sp_SugerirPuesto` — la escalera de 4 niveles del §8.5, sin reimplementar ninguna regla.** Orquesta `sp_ValidarAsignacion` (E4.6, ya traía `@permitir_ceder_perfil` desde entonces) y los campos que E3 ya había documentado para esto (`Puesto.TitularId`/`SexoPreferente`). Restricciones médicas nunca ceden en ningún nivel — gratis, porque `sp_ValidarAsignacion` no tiene forma de saltárselas. Alcance solo `rotativo` (los fijos ya los resolvió el barrido de §8.3). Código nuevo `SIN_PUESTOS_LIBRES` para el único hueco que los siete códigos de `sp_ValidarAsignacion` no cubrían — nunca silencio cuando no hay sugerencia. 11 pruebas nuevas — 210/210 en verde en todo el backend. Sin endpoint HTTP todavía: el catalogado en 05_TRD.md no tiene consumidor real hasta E6.8, cuando exista el flujo completo sugerir→confirmar→asignar |
| 2026-08-10 | E6.8 (1) | **E6 completa (8/8). `sp_AsignarPersona` — bloqueo determinista + idempotencia, literal sobre el pseudocódigo ya completo de 04 §7.3.** UPDLOCK+HOLDLOCK puesto-antes-que-persona (B1), `OperacionIdempotente` nueva, rechazo nominal solo para `PUESTO_OCUPADO`, rechazos auditados igual que éxitos dentro de la misma transacción. 9 pruebas nuevas incluidas dos con conexiones concurrentes reales (2 y 10 competidores por el mismo puesto, gana exactamente uno). Traídos también `GET /api/asignaciones/sugerencia` y `POST /api/puestos/{id}/asignar` — 7 pruebas HTTP nuevas. **Bug real encontrado**: `GetDbConnection().OpenAsync()` directo evita el interceptor de EF que fija `SESSION_CONTEXT` (04 §6.3) — la RLS de `Puesto`/`JornadaLinea` filtraba todo para quien no fuera coordinador. Existía también en `RegistradorAuditoria` (E2.5) desde siempre, invisible porque `Auditoria` no tiene RLS de fila. Corregido en ambos a `db.Database.OpenConnectionAsync()`. 226/226 en verde en todo el backend. Sin wiring de Android: el `LEE` de esta UT es solo el mecanismo de concurrencia — conectar el flujo completo en el teléfono queda pendiente antes de poder validar **PC-3** |
| 2026-08-10 | Integración de llenado (sin ID, no es UT del plan) | **Conecta E6.5-E6.8 en el teléfono — PC-3 queda listo para validar.** Botón de escanear en `MallaLineaScreen` → `FlujoAsignacionViewModel`/`FlujoAsignacionPorFicha` (nuevos, `ui/asignacion`) encadenan identidad (E6.6) → sugerencia (E6.7) → confirmación con clave de idempotencia (E6.8, 00 §B1) → refresco de la malla, sin reimplementar ninguna regla y sin tocar un solo archivo de las UTs ya verificadas. Mensajes de rechazo (`SIN_PUESTOS_LIBRES`, el nominal de B1) mostrados tal cual los devuelve el servidor. 11 pruebas de JVM + 5 de Compose nuevas — 78/78 de JVM y 32/32 de Compose en todo Android. Fixture descartable `SmartAssignAndroidJvmTest2` (puerto 5081) recreado a mano (migraciones, 6 usuarios, puesto/persona/restricción de E6.4/E6.6) para que las 18 pruebas de integración que dependen de él volvieran a verde. De paso, `SmartAssignDev` (la base real) también recibió las migraciones de E6.7/E6.8 que le faltaban — sin ellas `sp_SugerirPuesto`/`sp_AsignarPersona` no existían fuera de la base de pruebas |
| 2026-08-10 | E7.1 (1) | **Arranca E7 (Fatiga). El reloj de fatiga: `fn_MinutosEnPuesto` + `fn_UmbralFatigaSugeridoMinutos`/`fn_UmbralFatigaCriticoMinutos`.** Minutos desde `Asignacion.inicio` del ocupante activo, `NULL` (nunca 0) cuando la fatiga no aplica — fijo, inactivo o sin ocupante (§9.1). Umbral propio del puesto (00 §A4, ya en horas desde E3) convertido a minutos, con caída a `Parametro['fatiga_sugerido_default_min']`/`['fatiga_critico_default_min']` cuando el puesto no lo trae — sin ninguno de los dos, `NULL`: la regla no aplica todavía, ningún umbral inventado (R2), mismo criterio que `ventana_arranque_min` (E4.5). Deliberadamente sin clasificar en niveles ni calcular exceso relativo — eso es E7.2/E7.3. 9 pruebas nuevas — 235/235 en verde en todo el backend. Bug de prueba (no de las funciones): el primer intento no fijaba `SESSION_CONTEXT` antes de consultar `Puesto` (RLS), dejando `IF @tipo <> 'rotativo'` en `UNKNOWN` en vez de bloquear — mismo patrón de contexto de coordinador que ya usa el resto de la suite |
| 2026-08-10 | E7.2 (1) | **`fn_ExcesoRelativoFatiga` — el ordenamiento de fatiga nunca usa minutos absolutos (00 §A4/§B3).** `minutos_en_puesto / umbral_sugerido_minutos * 100`, `DECIMAL(6,2)`. Denominador siempre el umbral sugerido, el único punto de referencia para el avance continuo (03 §2). `NULL` sin denominador real (R2) — mismo criterio de ausencia que E7.1. Prueba dedicada del ejemplo normativo exacto de A4: 70 min/umbral 60 (116.67%) peor que 70 min/umbral 120 (58.33%). 7 pruebas nuevas — 242/242 en verde en todo el backend |
| 2026-08-10 | E7.3 (1) | **E7 casi completa (3/4). `fn_NivelFatiga` — normal/sugerido/crítico, reutilizando E7.1 sin reimplementarlo.** Crítico antes que sugerido; exactamente en el umbral ya cuenta como alcanzado; `NULL` sin ningún umbral calibrado (R2). "Fijos no entran en el cálculo" (§9.1/§5.1) y "fatiga del puesto, no de la categoría" (00 §A7) gratis por construcción — verificado con `[Theory]` sobre operario/operador_b/operador_c. 10 pruebas nuevas — 252/252 en verde. Ajuste de estabilidad en `ExcesoRelativoFatigaTests` (E7.2): tolerancia de porcentaje ampliada de 0.01 a 6 puntos — demasiado ajustada bajo la contención real de correr la suite completa en paralelo |
| 2026-08-10 | E7.4 (1) | **E7 completa (4/4).** `fn_MinutosEnPuestoEfectivos` (nueva, deja `fn_MinutosEnPuesto` intacto porque 01_PRD HU-D2 necesita el minuto literal en avisos) aplica el factor de doble turno (00 §B7, default **1.0 real** — el único de todo el motor) solo a clasificación/ordenamiento; `fn_ExcesoRelativoFatiga`/`fn_NivelFatiga` alterados internamente sin cambiar su contrato. 16 pruebas de backend nuevas — 260/260 en verde. Android: la malla expone `nivelFatiga`/`excesoFatiga`/`dobleTurno`; `TarjetaDePuesto` dibuja por fin la barra de fatiga que E6.4 dejó pendiente (grosor/color por nivel, pulso lento en crítico) más el distintivo de doble turno. 6 pruebas de Compose nuevas — 38/38 en verde con emulador (una prueba no relacionada, `ArranqueScreenTest`, resultó frágil corriendo la suite completa en un teléfono físico real conectado por USB — pasó aislada y en el emulador, mismo artefacto de dispositivo ya documentado en E6.5, no del código) |
| 2026-08-11 | E8.1 (1) | **Arranca E8 (Movimiento entre líneas). `Movimiento` (04 §5.2, completa) + `sp_DespacharPersona` — paso 1 de Parte X.** Rechaza sin ubicación física (`SIN_LINEA_FISICA`) o sin disponibilidad ("en Bolsón o sin asignar", literal — `NO_DISPONIBLE_PARA_DESPACHO`); bloqueo determinista bajo `UPDLOCK`+`HOLDLOCK` sobre `Personal` rechaza un segundo despacho mientras el primero sigue en tránsito (`YA_EN_TRANSITO`) — 5 despachos concurrentes reales, gana exactamente uno. `duracion_seg` calculada persistida y `DENY` sobre `Movimiento` (cerraba una nota pendiente de E4.7). Deliberadamente sin inmunidad real (E8.2) ni recepción (E8.3) todavía. 12 pruebas nuevas — 272/272 en verde en todo el backend |
| 2026-08-11 | E8.2 (1) | **Tránsito inmune (§6.1) — sin mecanismo nuevo, la inmunidad ya existía.** `sp_ValidarAsignacion` paso 2 (E4.6) y `UX_Mov_transito` (E8.1) ya bastaban; esta UT prueba con el camino real (despachar de verdad, no sembrar `situacion` a mano) que `sp_AsignarPersona` directo y la escalera `sp_SugerirPuesto` (E6.7) nunca capturan a quien está en tránsito — ni desde una línea ajena ni desde su propio destino ("su destino ya está comprometido") — y que `UX_Mov_transito` frena un `INSERT` crudo duplicado como última defensa de base de datos. De paso corrigió un test flaky de E8.1: el chequeo de `situacion` fuera de la transacción hace que los perdedores de una carrera real se repartan de forma no determinista entre dos códigos de rechazo válidos — la aserción exigía una distribución fija que dos corridas aisladas ya contradecían. 4 pruebas nuevas — 276/276 en verde en todo el backend |
| 2026-08-11 | E8.3 (1) | **`sp_RecibirPersona` — paso 3 de Parte X, "y solo entonces se le asigna el puesto": este SP no asigna nada, solo cierra el tránsito.** `hora_llegada` real, `estado='recibido'`, `recibido_por`; el destino decide la situación resultante vía `Linea.EsBolson` (nunca asumiendo que la línea 8 es el Bolsón por Id) — `en_bolson` o `presente_sin_asignar` según corresponda — y recién aquí se actualiza `linea_fisica_actual` (no en el despacho). 00 §C8: confirmación individual por `movimiento_id`, sin exigir categoría ni restricciones médicas (proporcionalidad — eso se valida al asignar). Todo el chequeo vive dentro de la transacción bajo `UPDLOCK`+`HOLDLOCK` desde el SELECT inicial, así que 5 confirmaciones concurrentes reales dan un resultado determinista (1 gana, 4 con `MOVIMIENTO_NO_EN_TRANSITO`) sin la variabilidad que E8.2 tuvo que tolerar en el despacho. 7 pruebas nuevas — 283/283 en verde en todo el backend |
